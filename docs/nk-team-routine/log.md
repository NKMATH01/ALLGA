# 올가 팀 루틴 로그

한 항목이 루틴 한 바퀴다. 최근 것을 위에 쌓는다.

## 2026-09-07 · D-4 중복 배포 정리 (데이터 삭제, 교차 감사)

- **저장 지점**: `b3b4989` (묶음 H push 직후)
- **조사 결과, 점검의 추정이 틀렸다**: "동일 배포 16지점 × 2건 이중 실행"이 아니라,
  같은 제목 시험이 둘(원본 `bd2f7c3f` 11-13 08:17, 재등록본 `f40165b2` 11-13 20:21)이고
  거기에 "미수등" 시험 `4fc68bc4`(20문항 40점)가 더해져, 셋이 각각 17개 지점 전체에
  2025-11-17 07:44 일괄 배포된 것이었다. 배포 52 = 18 + 17 + 17.
- **사용자 결정**: 응시 기록이 없는 배포만 삭제(재등록본 16 + 미수등 15 = 31).
  시험 기록 3개와 응시 3건(강남 김민수 재등록본 89점·AI 보고서 1, 송도·강남 "미수등"
  미제출 2)은 유지. 재등록본 정체는 "모름"이라 시험 기록은 남기고 불분명하다고 적었다.
- **감사 A(1차)**: 통과, 🟡4
- **감사 B(1차, 코덱스 GPT-6 Astra)**: **되돌림**, 🔴2 · 🟡1.
  🔴 는 선택→삭제 경쟁 조건 잠금 없음, 백업 파일 배타 생성 없음.
- **재작업**: `for update` 잠금 + DELETE 문 내 재검사, `flag:'wx'`, 접속 호스트 표시,
  저장소 루트 고정, 잔존 백업 안내, `process.exitCode`.
- **재감사 A**: 통과, 🟡2 (주석 과장, 잠금 대기 시간 미설정)
- **재감사 B**: 통과, ✅8 · 🟡1 (호스트 검사 `includes` 가 느슨함)
- **종합 판정**: 되돌림 1회 후 통과. 이후 `--apply` 1회 실행.
- **결과**(팀장 읽기 전용 대조): `exam_distributions` 52 → 21.
  `exam_attempts` 20 · `ai_reports` 10 · `distribution_students` 0 불변,
  원본 시험 배포 18 불변, 응시 붙은 배포 3개 생존, 남은 삭제 후보 0.
- **백업**: `docs/nk-team-routine/evidence/d4-deleted-distributions-2026-09-07.json`
  (31행, 배포 id·시험·지점·기간·생성 시각만. 개인정보 없음)
- **비용 어림**: 약 $5 (코덱스 2회 포함)
- **배운 것**: 교차 감사가 실제로 값어치를 했다. 코덱스가 잠금 부재를 🔴 로 잡았고,
  그 하나가 응시 기록을 CASCADE 로 날릴 수 있는 유일한 구멍이었다.

남긴 잔여: 강남·송도 학생 화면에는 응시가 붙은 배포 3개 때문에 같은 제목 시험이
여전히 보일 수 있다(의도된 보존). 재등록본·"미수등" 시험 기록은 관리자 시험 목록에
남아 있고, 정체를 확인한 뒤 별도로 결정한다.

## 2026-09-07 · 올가 점검 결함 묶음 H 마감(0009 적용·문서·push)

- **저장 지점**: `a8939b3` (묶음 H 7커밋이 로컬에 쌓인 상태에서 시작)
- **감사 A**: 90% (10건 중 통과 8, 부분 2)
- **기술 점검**: 심각 0, 보통 2
- **입장 점검**: 학생·지점장·관리자 전원 "괜찮음 ~ 고치면 좋음"
- **종합 판정**: 되돌림 1회 후 통과. 되돌린 항목 셋은
  판정 null 처리(등급 없는 학생에게 C '보완 필요' 를 지어내던 것),
  모달 헤더 배경 제거, 관리자 표 hover 첫 칸 보정이다.
- **비용 어림**: 약 $4

남긴 잔여: 모달 최대 폭 560px 미준수, `server/index.ts:125` 기동 로그 이모지,
`markReportFailed` 의 영향 행 수 미확인, `overflow-x-auto` 래퍼 없는 Branch 표
5곳(전부 데스크톱 전용 표라 실사용 영향 없음).
