# 올가 팀 루틴 로그

한 항목이 루틴 한 바퀴다. 최근 것을 위에 쌓는다.

## 2026-09-08 · D-8 미사용 컬럼 4개·조회 인덱스 2개 정리 (마이그레이션 0010, 클로드 점검)

- **저장 지점**: `c7529d0` (죽은 엔드포인트 문서 push 직후)
- **조사**: 참조 0·데이터 0 컬럼은 4개였다. `ai_reports.weak_areas`·`recommendations`·
  `expected_grade`(9행 전부 NULL), `exams.exam_file_url`(1행 NULL). 점검 목록에 함께
  적힌 `classes.is_active` 는 **사용 중**이라 목록이 틀렸다(`server/routes/classes.ts:26`,
  `BranchDashboard.tsx` 의 "운영 중/비활성"). `branches.is_active` 는 코드 참조 0 이지만
  전부 true 이고 지점 비활성 설계용이라 유지. 인덱스는 반대로 정정됐다. 조회에 쓰이는데
  없던 것은 `student_parents(parent_id)`(`parents.ts:40` 등 4곳)와
  `exam_attempts(distribution_id)`(`attempts.ts:100`·`distributions.ts:389` 등 9곳,
  점검 목록에 **없던** 항목)이고, 점검이 지목한 `ai_reports(student_id/exam_id)` 는
  조회 0(관계·연쇄 삭제만)이라 대상이 아니었다.
- **사용자 결정**: 컬럼 4개만 삭제, 인덱스 2개(`student_parents_parent_id_idx`,
  `exam_attempts_distribution_id_idx`) 추가. 점검 모드는 "클로드 점검".
- **감사 A(Opus)**: **통과 100%**(요구 9항목 전부 ✓), 🟡1 (가드 DO 블록이 삭제 대상
  컬럼을 직접 참조해, 적용 뒤 수동 재실행 시 "column does not exist" 로 실패한다.
  journal 이 재실행을 막고 트랜잭션이라 부분 적용도 없어 실무 위험 낮음, 미수정)
- **기술 점검(Sonnet)**: **통과**, 🟡1 (가드의 `IS NOT NULL` 은 SQL NULL 만 보므로 JSON
  리터럴 `null` 행이 있으면 과잉 차단. 데이터를 지키는 방향의 실패라 안전하고 실제 그런
  행은 없어 적용 성공). ✅ 로 확인된 것: 마이그레이터 단일 트랜잭션, journal `when`
  1788510616841 < 1788849729388 이라 적용 대상, 인덱스 정당성(기존 UNIQUE 는 student_id
  선두라 parent_id·distribution_id 단독 조회 미커버), 초소형 테이블이라 잠금 위험 없음,
  스키마 주석의 파일명 실재.
- **종합 판정**: 통과, 되돌림 0회. 화면 변화가 없어 입장 점검은 생략.
- **적용**: 사용자가 `npm run db:migrate` 를 1회 직접 실행(2026-09-08 16시경).
  세션 안전장치가 실행 위임을 막아 사용자가 직접 눌렀다.
- **결과**: `c4d04b6` 4파일(`drizzle/0010_keen_zarek.sql`,
  `drizzle/meta/0010_snapshot.json`, `drizzle/meta/_journal.json`, `server/db/schema.ts`).
  SQL 은 가드 `DO $$` → `CREATE INDEX IF NOT EXISTS` 2개 → `DROP COLUMN IF EXISTS` 4개
  순서다. 전후 대조(팀장 읽기 전용): `__drizzle_migrations` 10 → **11**(last
  1788849729388), 삭제 대상 4열 **부재**, `ai_reports` 13 → **10열**, `exams` 13 →
  **12열**, 새 인덱스 0 → **2**, 유지 컬럼 전후 실존, 행수 exams 1 · ai_reports 9 ·
  exam_attempts 17 · student_parents 1 · exam_distributions 18 · users 30 **전부 불변**.
- **게이트**: 서버 tsc 0 · 클라이언트 tsc 0 · vitest 5파일 87건 · 하드코딩 hex 0
  (주석 1, `BranchDashboard.tsx:3628`, D-8 이전부터) · `npx drizzle-kit check`
  "Everything's fine" · 삭제 컬럼 코드 참조 0(`reports.ts:1164-1183` 의
  `recommendations` 는 요약 계산 지역 변수).
- **비용 어림**: 약 $2
- **배운 것**: ① 세션이 끊기면 `state.json` 이 아니라 메모리 파일로 재개했다. 노드마다
  `state.json` 을 갱신하지 않은 탓이니 다음 루틴부터는 구현 커밋 직후에 바로 갱신한다.
  ② 컬럼 삭제는 0009 와 순서가 반대다. 옛 코드가 도는 서버가 있으면 코드 배포가 먼저,
  마이그레이션이 나중이어야 한다(이 저장소는 배포 설정이 없고 서버를 로컬에서 띄우므로
  해당 없음).
- **잔여**: 감사 🟡2 미수정. `docs/02-design/data-model.md` 에 0007 `target_kind` 와
  0009 `status`·`failure_reason` 이 빠져 있는 기존 드리프트가 있다(이번 범위 밖, 기록만).

## 2026-09-07 · 죽은 엔드포인트 7개 제거 (코드만, 클로드 점검)

- **저장 지점**: `719dc8c` (E-3 문서 push 직후)
- **조사**: `server/index.ts` 마운트 8줄에서 라우트 61개(+`/health`)를 펼치고
  `client/src` 의 호출 경로를 전수로 뽑아 맞췄다. 화면이 한 번도 부르지 않는 주소가
  **12개**. 점검 당시 §7 목록과 구성이 다르다. P-4 로 `impersonate/restore`·`/student`
  는 실사용이 됐고, 이중 마운트에 가려져 있던 `GET /students/stats` 가 새로 드러났다.
  12개를 A(대체 경로 있음 7) · B(학부모 API 3) · C(표준 단건 조회 2)로 나눴다.
- **사용자 결정**: "A 만 7개 삭제". B 는 학부모 계정 생성 UI 가 없어 유일 경로라 유지,
  C 는 P-1·P-2 가 권한을 좁혀 둔 표준 조회라 유지.
- **감사 A(Opus)**: 통과 98%, 🟡1 (`schema.ts:59` 주석이 옛 경로 언급, 스키마 무변경
  지시라 방치)
- **기술 점검(Sonnet)**: 통과, 🟡3 (학생이 옛 `/exams/available` 을 치면 403,
  `students.ts:312` 제거 주석의 경로 표기 부정확, "대체: `GET /api/students`" 는
  `latestExam` 필드가 없어 완전 대체 아님)
- **종합 판정**: 되돌림 0회 통과. 뒤의 🟡2 는 `cd7775a` 에서 주석 문구를 고쳐 해소.
- **결과**: `19443c0` 7파일 +25/-341, 라우트 **61 → 54**, `/api/branch-students`
  이중 마운트 제거, 미사용 import 정리, `api-spec.md` §4.3·§8.7·§10.2 에
  "(2026-09-07 제거)"와 대체 경로 표기.
- **게이트**: 서버 tsc 0 · 클라이언트 tsc 0 · vitest 87 · 하드코딩 hex 0.
- **런타임**(서버 기동, 지점장 로그인 1회): 지운 8주소 404. 단
  `/api/exams/available` 은 `GET /exams/:id` 로 폴스루해 401(라우트는 사라진 게 맞다).
  이웃 주소는 미인증 401 → 로그인 후 200.
- **비용 어림**: 약 $2
- **배운 것**: 죽은 코드도 유일 경로(학부모 API)와 표준 조회는 남긴다.
  그리고 라우트 수 기준선은 지시 전에 직접 세어 둘 것. 문서의 59 와 실제 61 이 달랐다.

## 2026-09-07 · E-3 중복 시험 기록 정리 (해설 이식 + 삭제, 교차 감사)

- **저장 지점**: `0e31326` (D-4 문서 push 직후)
- **조사**: 세 시험 전부 관리자 `allga` 가 만들었다. "미수등"(`4fc68bc4`, 20문항 40점)
  20:20, 재등록본(`f40165b2`) 그 1분 뒤 20:21 → 등록 기능 테스트 흔적이다.
  재등록본은 원본(`bd2f7c3f`)과 45문항 중 37 동일, 8문항(7·12·14·20·24·30·33·36)의
  `explanation`·`commentary` 만 더 길다(평균 63 → 126자). 응시 3건은 전부 테스트 계정
  (김민수 = `server/db/seed.ts` 시드 학생. 재등록본 응시는 배포 2분 뒤 1초 만에 제출된
  89점 + AI 보고서 1). 실제 학생이 있는 지점은 둔산점 11 · 송도 1 뿐이다.
- **사용자 결정**: 원본 8문항 해설을 재등록본 값으로 이식 → 재등록본 삭제 → "미수등" 삭제.
- **감사 B(코덱스 GPT-6 Astra)**: 통과, ✅8 · 🟡2 (자식 행 미잠금, 부분 백업 파일 안내 누락)
- **감사 A(Opus)**: **되돌림**, 🔴1 · 🟡2. 🔴 은 "`tx.json()` 이 jsonb 라 `json` 컬럼
  대입이 실패한다"였고 스스로 **추측**이라 표기했다.
- **팀장 반박(실행 없음)**: `pg_cast` 에 jsonb → json 대입 캐스트(`castcontext='a'`) 존재,
  0행 UPDATE 를 롤백 트랜잭션에서 돌려 문장 성립 확인(쓰기 0). jsonb 왕복으로 JSON 텍스트가
  정규화되는 것(키 순서·공백, 18305 → 19384자)은 사실이나 값은 불변이고 스크립트가
  재조회로 대조한다. → 🔴 기각.
- **종합 판정**: 되돌림 0회 통과. `--apply` 1회 실행.
- **결과**(팀장 읽기 전용 대조): `exams` 3 → **1**, `exam_distributions` 21 → **18**,
  `exam_attempts` 20 → **17**, `ai_reports` 10 → **9**, `students` 12 불변.
  원본 배포 18 · 응시 17 · 보고서 9 불변, 채점 필드 지문 불변,
  원본 해설 평균 63 → 126자 · `commentary` 126 → 188자.
- **백업**: `docs/nk-team-routine/evidence/e3-exam-cleanup-2026-09-07.json` (349 KB).
  학생 답안과 보고서 HTML 이 들어 있어 **git 미추적**으로 두고 `.gitignore` 에 규칙을 넣었다.
- **비용 어림**: 약 $5
- **배운 것**: 감사의 "추측" 표기 지적은 팀장이 실행 없이(`pg_cast` 조회 · 롤백 트랜잭션)
  검증해 되돌림 낭비를 막았다. 그리고 개인정보가 든 백업은 커밋하지 않는다.

남긴 잔여: 감사 B 의 🟡2 는 스크립트 개선 여지로 고치지 않았다. 재등록본의 정체는
끝내 "모름"이지만 기록이 삭제되어 종결한다.

## 2026-09-07 · D-4 중복 배포 정리 (데이터 삭제, 교차 감사)

- **저장 지점**: `b3b4989` (묶음 H push 직후)
- **조사 결과, 점검의 추정이 틀렸다**: "동일 배포 16지점 × 2건 이중 실행"이 아니라,
  같은 제목 시험이 둘(원본 `bd2f7c3f` 11-13 08:17, 재등록본 `f40165b2` 11-13 20:21)이고
  거기에 "미수등" 시험 `4fc68bc4`(20문항 40점)가 더해져, 셋이 각각 17개 지점 전체에
  2025-11-17 07:44 일괄 배포된 것이었다. 배포 52 = 18 + 17 + 17.
- **사용자 결정**: 응시 기록이 없는 배포만 삭제(재등록본 16 + 미수등 15 = 31).
  시험 기록 3개와 응시 3건(강남 김민수 재등록본 89점·AI 보고서 1, 송도·강남 "미수등"
  미제출 2)은 유지. 재등록본 정체는 "모름"이라 시험 기록은 남기고 불분명하다고 적었다.
- **감사 A(1차)**: 통과, 🟡4
- **감사 B(1차, 코덱스 GPT-6 Astra)**: **되돌림**, 🔴2 · 🟡1.
  🔴 는 선택→삭제 경쟁 조건 잠금 없음, 백업 파일 배타 생성 없음.
- **재작업**: `for update` 잠금 + DELETE 문 내 재검사, `flag:'wx'`, 접속 호스트 표시,
  저장소 루트 고정, 잔존 백업 안내, `process.exitCode`.
- **재감사 A**: 통과, 🟡2 (주석 과장, 잠금 대기 시간 미설정)
- **재감사 B**: 통과, ✅8 · 🟡1 (호스트 검사 `includes` 가 느슨함)
- **종합 판정**: 되돌림 1회 후 통과. 이후 `--apply` 1회 실행.
- **결과**(팀장 읽기 전용 대조): `exam_distributions` 52 → 21.
  `exam_attempts` 20 · `ai_reports` 10 · `distribution_students` 0 불변,
  원본 시험 배포 18 불변, 응시 붙은 배포 3개 생존, 남은 삭제 후보 0.
- **백업**: `docs/nk-team-routine/evidence/d4-deleted-distributions-2026-09-07.json`
  (31행, 배포 id·시험·지점·기간·생성 시각만. 개인정보 없음)
- **비용 어림**: 약 $5 (코덱스 2회 포함)
- **배운 것**: 교차 감사가 실제로 값어치를 했다. 코덱스가 잠금 부재를 🔴 로 잡았고,
  그 하나가 응시 기록을 CASCADE 로 날릴 수 있는 유일한 구멍이었다.

남긴 잔여: 강남·송도 학생 화면에는 응시가 붙은 배포 3개 때문에 같은 제목 시험이
여전히 보일 수 있다(의도된 보존). 재등록본·"미수등" 시험 기록은 관리자 시험 목록에
남아 있고, 정체를 확인한 뒤 별도로 결정한다.

## 2026-09-07 · 올가 점검 결함 묶음 H 마감(0009 적용·문서·push)

- **저장 지점**: `a8939b3` (묶음 H 7커밋이 로컬에 쌓인 상태에서 시작)
- **감사 A**: 90% (10건 중 통과 8, 부분 2)
- **기술 점검**: 심각 0, 보통 2
- **입장 점검**: 학생·지점장·관리자 전원 "괜찮음 ~ 고치면 좋음"
- **종합 판정**: 되돌림 1회 후 통과. 되돌린 항목 셋은
  판정 null 처리(등급 없는 학생에게 C '보완 필요' 를 지어내던 것),
  모달 헤더 배경 제거, 관리자 표 hover 첫 칸 보정이다.
- **비용 어림**: 약 $4

남긴 잔여: 모달 최대 폭 560px 미준수, `server/index.ts:125` 기동 로그 이모지,
`markReportFailed` 의 영향 행 수 미확인, `overflow-x-auto` 래퍼 없는 Branch 표
5곳(전부 데스크톱 전용 표라 실사용 영향 없음).
