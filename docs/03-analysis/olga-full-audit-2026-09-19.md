# 올가 미수등 시스템 전체 점검 2차 (2026-09-19): 디자인 → 화면 → 서버·권한 → DB

- **점검일**: 2026-09-19
- **기준선**: `7369457` (2026-09-08 D-8 마감, `origin/main`)
- **점검 범위**: 기준선 이후 로컬 5커밋
  - `5c5f57d` 전체 앱 프리미엄 디자인 개편
  - `c125076` 지점 반별 성적 탐색·0점 표시 보완
  - `aed233a` 웹 보고서 읽기 화면 `/reports/:reportId` (저장 지점 3커밋)
  - `fd8be58` 총괄 관리자 분석 패널(LearnWorlds 참고)·관리 화면 색 통일·총괄→지점 바로가기
  - `ef594a4` 로그인 오류 구분 안내
  - 미커밋 작업을 먼저 위 3커밋으로 저장한 뒤 점검을 시작했다.
- **팀·방식**: 점검 팀장 Fable 5.1 이 계획·DB 실측·런타임 확인을 직접 수행. 점검단은 기술 ① 서버·권한(Sonnet), 기술 ② 화면·디자인 헌법(Sonnet), 입장 점검 학생·학부모·지점장·총괄(Haiku 4), 운영·비용(Haiku). **전원 읽기 전용**이며 상태를 바꾸는 API·SQL 은 실행하지 않았다.
- **팀장 게이트**: 서버 tsc 0 · 클라이언트 tsc 0 · vitest **11파일 113건** · 화면 하드코딩 hex 0(주석 1건 제외) · `drizzle-kit generate` "No schema changes".

---

## 1. 건강 판정 보드

판정 규칙은 1차 점검과 같다. **위험** = 🔴 1건 이상 / **주의** = 🟡 1건 이상 또는 미검증 과다 / **양호** = 그 외.

| 축 | 판정 | 결정 근거 |
|---|---|---|
| 성적 무결성 | **양호** | 서버 채점·제출 경로 무변경. 0점이 사라지던 문제는 `?? null` 로 수정됨 |
| 권한 경계 | **양호** | 런타임 12건 확인. 미인증 5경로 401, 제거된 경로 404, admin `/distributions/students` 403, 총괄→지점 impersonate 정상(`originalUser=admin`), 지점 세션의 타 지점 배포 404, 학생 세션 `GET /exams` 정답키 403, 타 학생 보고서 summary 403, 본인 보고서 summary·인쇄 HTML 200, restore 정상 |
| 데이터 정합 | **양호** | 스키마 ↔ 스냅샷 정합. 무결성 점검 6종 전부 0건(§2) |
| 빌드·기동 | 주의 | 번들 약 770KB, 코드 분할 없음. 기존 부채이며 이번 회귀 아님(N-9) |
| 화면·지면 헌법 | 주의 | `DESIGN.md` 1.3 표가 최신 결정과 어긋남, 화면 문구에 긴 줄표 1건, "미산출" 설명 없음(N-1·N-5·N-6) |

**한 줄 요약**: 이번 5커밋에서 성적·권한·데이터를 건드린 회귀는 없다. 남은 것은 화면 문구·일관성과 문서 갱신이며 전부 🟡 이다.

---

## 2. DB 실측 (읽기 전용, 2026-09-19)

- **마이그레이션**: 11개 적용(last `1788849729388`). 스키마 ↔ 스냅샷 정합, `drizzle-kit generate` 결과 "No schema changes".
- **행수**: users 30(admin 1 · branch 16 · parent 1 · student 12, 전부 active) / students 12(둔산점 11 · 송도 1) / exams 1 / exam_distributions 18 / exam_attempts 17(제출 15 · 미제출 2) / ai_reports 9(전부 completed, metaVersion v2 6 · v3 3) / student_parents 1 / classes 1 / session 1.
- **제약**: CHECK 2(`ai_reports.status`, `exam_distributions.target_kind`), UNIQUE(`users.username`, `students.user_id`, `parents.user_id`, `exam_attempts(student,distribution)`, `student_classes`, `student_parents`, `ai_reports.attempt_id`), FK 전부 존재, 인덱스 30개.
- **무결성 전부 0건**: 지점 고아 users 0 / students 없는 student user 0 / 제출됐는데 점수 없음 0 / completed 인데 html 없음 0 / 미완료 보고서 0 / students 종류인데 배정 없는 배포 0.
- **기타**: 0점 응시 0건이라 0점 표시 수정은 운영 데이터로 검증하지 못했다. users 는 `updated_at <> created_at` 이 0건이며, `$onUpdate` 는 갱신 시에만 동작하므로 갱신 이력이 없다는 뜻이다.

---

## 3. 결함·관찰 표

표기: **ID · 심각도 · 위치 · 관측 · 기대 · 영향 · 권장**. 이번 점검의 결함은 **전부 🟡** 이고 🔴 은 없다.

| ID | 심각도 | 위치 | 관측 | 기대 | 영향 | 권장 |
|---|---|---|---|---|---|---|
| **N-1** | 🟡 화면 | `client/src/components/ReportReadingView.tsx:34` | 등급이 없으면 "미산출" 만 보여 주고 이유를 적지 않는다. 같은 상황에서 인쇄 지면 HTML 은 "판정 없음 · 등급 기준 축적 중" 을 쓴다(`server/templates/newReportTemplate.ts:134,992,997`) | 웹 화면과 지면이 같은 문구·같은 설명을 쓴다 | 학생·학부모가 등급이 빠진 이유를 오해한다(학생 입장 점검에서 지적) | 지면과 같은 문구를 쓰고 한 줄 설명을 붙인다 |
| **N-2** | 🟡 화면 | `client/src/pages/WebReportPage.tsx:13` | 뒤로가기 링크 문구가 "성적 화면" 인데 실제 이동 대상은 `/`(역할별 홈)이다 | 문구와 이동 대상이 일치한다 | 학생·학부모가 다른 화면을 기대한다 | 문구를 "← 돌아가기" 로 바꾼다 |
| **N-3** | 🟡 화면 | `client/src/pages/AdminDashboard.tsx:1631` · `:634` | 같은 `impersonateBranchMutation` 을 부르는 두 진입점의 방식이 다르다. 헤더 드롭다운은 확인창 없이 즉시 이동하고, 지점 표의 "로그인" 버튼은 `confirm` 을 띄운다 | 같은 동작은 같은 방식으로 확인한다 | 총괄이 드롭다운을 잘못 건드리면 확인 없이 지점 세션으로 전환된다(총괄 입장 점검에서 지적) | 확인 방식을 하나로 통일한다 |
| **N-4** | 🟡 화면 | `client/src/components/AdminAnalytics.tsx:9` | 지점 비교가 상위 6개 고정인데 학생이 있는 지점은 2개뿐이라(나머지 15개는 0명) 차트 절반이 0 막대로 채워진다 | 의미 있는 비교 대상만 보여 준다 | 총괄 분석 패널의 정보 밀도가 낮다 | 0명 지점을 제외하거나 "학생 있는 지점 N개" 를 표기한다 |
| **N-5** | 🟡 헌법 | `DESIGN.md:105` (1.3 표 "지점·관리자" 행) | 그린 예산 서술이 최신 결정(관리 화면 보라 `.management-theme`, 2026-09-08·09-09 사용자 승인)과 어긋난다 | 헌법 표가 현재 결정을 반영한다 | 이후 감사가 옛 기준으로 판정한다 | 표를 갱신한다. 위반이 아니라 개정이다 |
| **N-6** | 🟡 헌법 | `client/src/components/ReportReadingView.tsx:33` (원점수 `<dd>`) | 화면 문구에 긴 줄표(em-dash) 문자가 1건 남아 있다 | 한국어 화면 문구에 긴 줄표를 쓰지 않는다 | 헌법 위반 1건 | 일반 문장부호로 바꾼다 |
| **N-7** | 🟡 서버 | `server/utils/distributionStudentRow.ts:24` | `grade: attempt?.grade \|\| null` 이라 등급 0 이 들어오면 `score` 와 같은 문제가 생긴다. 현재 데이터에 grade 0 은 없다 | 0 과 없음을 구분한다 | 잠재 결함이며 현재 재현되지 않음 | `?? null` 로 바꾼다 |
| **N-8** | 🟡 문서 | `docs/02-design/data-model.md` | 0007 `target_kind`, 0009 `status`·`failure_reason`, 0010 컬럼 삭제와 인덱스 2개가 반영되지 않았다(문서 내 참조 0건) | 문서가 적용된 마이그레이션을 반영한다 | 설계 문서를 믿고 작업하면 어긋난다. D-8 회차부터 이어진 기존 드리프트 | 갱신한다 |
| **N-9** | 🟡 운영 | `client/src/pages/BranchDashboard.tsx`(3938줄) · `client/src/pages/AdminDashboard.tsx`(1661줄) | 코드 분할이 없어 번들이 약 770KB 다 | 역할별로 필요한 코드만 받는다 | 첫 로딩이 느리다. 기존 부채이며 이번 회귀 아님 | `React.lazy` 로 역할별 분할. 별도 작업으로 뺀다 |

**관찰(결함 아님)**

- 총괄 → 지점 → 학생 순으로 전환한 뒤 restore 하면 지점이 아니라 총괄로 바로 돌아온다. `originalUser` 가 1단계만 기억하는 설계 때문이다.
- 학부모 화면의 응시 수와 학생 화면의 수가 다른 것은 "제출본만" 과 "배포 전체" 의 차이이며 설계상 정상이다.
- impersonate 뒤 전체 새로고침은 세션 전환을 안전하게 마치기 위한 것이다.

---

## 4. 입장 점검 소감

| 입장 | 한 줄 소감 |
|---|---|
| 학생 | 흐름은 좋다. "미산출" 설명과 뒤로가기 문구를 고치면 좋겠다 |
| 학부모 | 괜찮다. 고칠 것이 없다 |
| 지점장 | 괜찮다. 0점·미응시·대상 아님이 명확히 구분되고 클릭 2~3번이면 닿는다 |
| 총괄 | 고치면 좋겠다. 드롭다운 확인창과 상위 6개 고정 |

---

## 5. 커버리지

| 구분 | 내용 |
|---|---|
| 런타임 확인 | 권한 경계 12건, 보고서 열람 권한, 총괄→지점→학생 전환 사슬과 복귀 |
| 정적 확인 | 화면 접근성(화면별 `h1` 각 1개, label 연결 24/24 · 18/18 · 3/3, 표 첫 열 sticky, dialog `h2`), 1차 점검 U-3 · U-11 · U-16 회귀 없음 |
| 미검증 | 실제 브라우저 화면(디자인 시각 품질·다크·390px)은 이번 점검에서 실시하지 않았다. 이전 루틴 로그에 일부 확인 기록이 있다. 0점 표시는 운영에 0점 응시가 없어 검증하지 못했다 |

---

## 6. 판정

**통과**. 🔴 0건 · 🟡 9건.

후속 후보는 두 묶음이다.

1. N-1 ~ N-8 소수정 묶음(코드 6곳 · 문서 2곳)
2. N-9 코드 분할은 범위가 달라 별도 작업으로 뺀다
