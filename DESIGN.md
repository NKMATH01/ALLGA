# 올가 미수등 시스템 디자인 헌법

입시 국어 학원의 시험·성적 관리 프로그램을 위한 단일 디자인 규범입니다. 이 문서는 취향의 기록이 아니라 구속력 있는 규칙이며, 새 화면을 만들거나 기존 화면을 고칠 때 여기 적힌 토큰과 문법만 사용합니다.

기준 구현: 앱 화면은 `reference/work-report-mockup.html`, 보고서 지면은 `design-migration/after-13-report-v4.html` (10장). 문서와 구현이 어긋나면 이 문서가 우선하고, 구현을 고칩니다.

---

## 1. 철학과 북극성

### 1.1 북극성

> **학부모가 화면을 캡처해 카톡으로 보냈을 때, 학원이 신뢰할 만한 곳으로 보일 것.**

이 프로그램의 화면은 대부분 학생의 성적이라는 민감한 정보를 다룹니다. 화면이 들뜨고 화려하면 데이터의 무게가 가벼워 보이고, 지나치게 삭막하면 학원이 성의 없어 보입니다. 목표는 **차분한 권위**입니다.

### 1.2 이원화 원칙 (二元化)

색은 두 개의 역할로 나뉘며, 역할을 넘나들지 않습니다.

| 계열 | 역할 | 쓰는 곳 | 절대 쓰지 않는 곳 |
|---|---|---|---|
| **네이비** | 구조와 권위 | 사이드바, 헤더, 주 버튼, 표 머리, 활성 내비, 포커스 링 | 성취의 강조, 상태 표시 |
| **브라스** | 성취와 강조 | 최고 기록, 등급 상승폭, 화면의 단 하나뿐인 핵심 CTA | 배경 면, 구조선, 일반 버튼, 아이콘 기본색, 상태 표시 |

**네이비는 그릇이고, 브라스는 그릇에 담긴 성취입니다.** 네이비가 넓은 면과 구조를 맡기 때문에, 브라스는 좁고 드물게 등장할 때만 성취로 읽힙니다.

### 1.3 브라스 2곳 규칙 (강제)

> **한 화면(뷰포트 아닌 라우트 단위)에 브라스는 최대 2곳까지만 등장한다.**

- 2곳을 세는 단위는 "시각적으로 인지되는 브라스 덩어리"입니다. 숫자 하나 + 그 숫자에 붙은 단위/화살표는 1곳으로 셉니다.
- 3곳째가 필요하다고 느껴지면, 그것은 브라스가 부족한 게 아니라 **화면의 초점이 흐린 것**입니다. 우선순위를 다시 정하고 2곳으로 줄입니다.
- 표의 셀, 리스트의 반복 항목에는 브라스를 쓰지 않습니다. 반복되는 순간 강조가 아니라 장식이 됩니다.
- 예외: 인쇄용 보고서 지면(10장)은 지면당 2곳으로 셉니다.

### 1.4 금지 목록

이 프로젝트에서 다음은 예외 없이 금지합니다.

- **그라디언트 배경 전면 금지.** `bg-gradient-*`, `linear-gradient`로 칠한 카드, 헤더, 버튼, 사이드바 모두 해당. 색은 단색 면과 1px 선으로만 구분합니다.
- **화면마다 다른 아이덴티티 색.** 현재 로그인/전체관리자는 오렌지레드, 지점은 인디고블루, 학생은 퍼플로 갈라져 있습니다. 전부 네이비로 통합합니다(9장).
- **텍스트 그라디언트** (`bg-clip-text text-transparent`).
- **네온 글로우, 컬러 그림자** (`shadow-blue-500` 류). 그림자는 네이비 틴트의 중성 그림자만.
- **이모지.** 학부모에게 전달되는 문서형 화면이므로 텍스트와 아이콘으로만 표현합니다.
- **em-dash(U+2014), en-dash(U+2013).** 하이픈(-)만 사용합니다. 이 문서가 두 문자를 코드포인트로만 적는 이유도 같습니다. 코드와 문서 모두 `grep`으로 0건이 확인되어야 합니다.
- **원색 채도의 큰 면.** 빨강/초록 배경 카드로 상태를 표현하지 않습니다. 상태는 뱃지와 1px 테두리로.

---

## 2. 컬러 토큰 (3계층)

계층은 아래로만 참조합니다. **컴포넌트는 2계층과 3계층만 참조하고, 1계층 브랜드 스케일을 직접 쓰지 않습니다.** 1계층은 2계층을 정의하기 위해서만 존재합니다.

```
① 브랜드 원색 스케일 (navy, brass)
        ↓ 참조
② 시맨틱 토큰 (surface, text, border, action)   ③ 기능 토큰 (success/warning/error/info)
        ↓ 참조                                          ↓ 참조
              컴포넌트 / 화면
```

③ 기능 계층은 ①을 참조하지 않습니다. **브랜드 색이 네이비에서 무엇으로 바뀌더라도 기능색의 의미와 값은 그대로 유지되어야 합니다.** 등급, 합격 여부, 오류는 브랜드의 문제가 아니라 의미의 문제이기 때문입니다.

### 2.1 ① 브랜드 계층

```css
:root {
  /* 네이비: 구조와 권위 */
  --navy-50:  #F4F6FA;
  --navy-100: #E6EBF4;
  --navy-200: #C9D4E7;
  --navy-300: #A3B4D3;
  --navy-400: #7189B5;
  --navy-500: #4C6595;
  --navy-600: #354C78;
  --navy-700: #27395C;
  --navy-800: #1B2942;   /* 헤더, 사이드바 기본 */
  --navy-900: #131E31;
  --navy-950: #0C1421;   /* 야간 모드 캔버스 */

  /* 브라스: 성취와 강조 */
  --brass-300: #E3C88E;
  --brass-400: #D4B26A;  /* 어두운 면 위의 브라스 텍스트 */
  --brass-500: #C09A4E;  /* 브라스 기준색, 면과 선 */
  --brass-600: #A07F3B;  /* 큰 텍스트(18px+) 전용 */
  --brass-700: #7D622C;  /* 밝은 면 위의 본문 크기 브라스 텍스트 */
}
```

**브라스 명도 규칙 (접근성).** 흰 면 위에서 `--brass-500`, `--brass-600`은 본문 크기 텍스트로 쓸 수 없습니다(대비 4.5:1 미달). 밝은 면 위 텍스트는 `--brass-700`(대비 5.8:1), 어두운 면 위 텍스트는 `--brass-400`(navy-800 대비 7.2:1)을 씁니다. `--brass-500`은 면, 선, 큰 숫자(24px 이상)에만.

### 2.2 ② 시맨틱 계층 (라이트 기준)

```css
:root {
  /* 면 */
  --surface:          #FFFFFF;   /* 카드, 패널 */
  --surface-sunken:   var(--navy-50);   /* 앱 캔버스 */
  --surface-raised:   #FFFFFF;   /* 모달, 팝오버, 드롭다운 */
  --surface-inverse:  var(--navy-800);  /* 헤더, 사이드바 */
  --surface-subtle:   var(--navy-100);  /* 표 머리, 비활성 탭 */

  /* 글자 */
  --text-primary:     var(--navy-900);
  --text-secondary:   var(--navy-600);
  --text-tertiary:    var(--navy-400);  /* 캡션, 단위, 보조 라벨 */
  --text-on-inverse:  #FFFFFF;
  --text-on-inverse-muted: var(--navy-200);
  --text-accent:      var(--brass-700); /* 성취 텍스트, 2곳 규칙 적용 */

  /* 선 */
  --border:           var(--navy-200);  /* 기본 구획선 */
  --border-strong:    var(--navy-300);  /* 표 바깥선, 입력 테두리 */
  --border-subtle:    var(--navy-100);  /* 리스트 행 구분선 */
  --border-inverse:   var(--navy-700);

  /* 액션 */
  --action:           var(--navy-800);
  --action-hover:     var(--navy-700);
  --action-active:    var(--navy-900);
  --action-text:      #FFFFFF;
  --action-subtle:    var(--navy-100);
  --action-subtle-hover: var(--navy-200);
  --focus-ring:       var(--navy-500);

  /* 강조 (브라스, 2곳 규칙 적용 대상) */
  --accent:           var(--brass-500);
  --accent-strong:    var(--brass-700);
  --accent-surface:   #FBF6EA;   /* 브라스 계열의 아주 옅은 면 */

  /* 그림자 (네이비 틴트, 중성 검정 금지) */
  --shadow-sm: 0 1px 2px rgba(19, 30, 49, 0.06);
  --shadow-md: 0 2px 8px rgba(19, 30, 49, 0.08);
  --shadow-lg: 0 8px 24px rgba(19, 30, 49, 0.12);
}
```

### 2.3 ③ 기능 계층 (브랜드 독립)

```css
:root {
  --fn-success:         #1D7A4C;
  --fn-success-surface: #E8F4EE;
  --fn-success-border:  #A8D4BE;

  --fn-warning:         #8F5A00;
  --fn-warning-surface: #FBF1DF;
  --fn-warning-border:  #E0C48A;

  --fn-error:           #B3261E;
  --fn-error-surface:   #FCEDEC;
  --fn-error-border:    #E9B4B0;

  --fn-info:            #0F6E7A;
  --fn-info-surface:    #E6F2F4;
  --fn-info-border:     #9FCBD2;
}
```

네 색 모두 흰 면 위 대비 5:1 이상입니다.

**info를 청록으로 잡은 이유:** 파랑 계열 info는 네이비 구조색과 혼동됩니다. 사용자가 "이 파랑이 구조인가 상태인가"를 판단해야 하는 순간 이원화가 무너지므로, info는 네이비에서 확실히 떨어진 청록으로 고정합니다.

### 2.4 등급 색 규칙 (1 ~ 9등급)

등급은 성적이지 시스템 상태가 아닙니다. 따라서 **등급에는 `--fn-error`를 쓰지 않습니다.** error는 시스템 오류와 파괴적 동작에만 남겨둡니다.

| 등급 | 의미 라벨 | 토큰 |
|---|---|---|
| 1 ~ 2 | 우수 | `--fn-success` 계열 |
| 3 ~ 4 | 양호 | `--fn-info` 계열 |
| 5 ~ 6 | 보통 | `--text-secondary` + `--border` (무채색) |
| 7 ~ 9 | 보완 필요 | `--fn-warning` 계열 |

등급 뱃지는 항상 `면(-surface) + 1px 테두리(-border) + 글자(기본색)` 3종 세트로 구성하며, 원색 배경에 흰 글자를 쓰지 않습니다. 7~9등급에 빨강을 쓰지 않는 것은 미학이 아니라 정책입니다. 학생에게 보이는 화면이기 때문입니다.

**예외: 교사 관리 화면 한정 등급 스케일.** 지점·전체 관리자가 명단을 훑는 화면에서는 위 색 배정이 등급 차이를 거의 드러내지 못합니다(2·3·4등급이 모두 옅은 톤이라 한 덩어리로 보입니다). 교사용 화면에 한해 아래 스케일을 씁니다.

| 등급 | 교사 화면 표기 |
|---|---|
| 1 ~ 2 | `--fn-success` **면** + `--text-on-inverse` 글자 (이 스케일에서만 원색 면 허용) |
| 3 ~ 4 | `--fn-success-surface` 면 + `--fn-success` 글자 |
| 5 ~ 6 | `--fn-warning-surface` 면 + `--fn-warning` 글자 |
| 7 ~ 9 | `--surface-subtle` 면 + `--text-secondary` 글자 (중성 회색, **빨강 금지 유지**) |

**적용 범위는 교사 화면뿐입니다.** 학생 화면, 학부모 화면, AI 보고서 지면은 위 기본 배정을 그대로 씁니다. 7~9등급에 빨강을 쓰지 않는 원칙은 두 경로 모두에서 유지됩니다 - 낙인 색을 피하려는 규칙이지 대비를 포기하려는 규칙이 아닙니다. 구현은 `gradeBadgeClass`(학생·학부모·보고서)와 `gradeBadgeOperate`(교사 관리)로 함수를 나눠 섞이지 않게 합니다.

**상태 뱃지에 성공색을 쓰지 않습니다.** `제출 완료`처럼 **정상이 기본값인 상태**는 무채색으로 둡니다. 목록의 대다수 행이 초록이면 초록은 정보가 아니라 배경이 됩니다. 색은 벗어난 것(`작성 중` 앰버, 참고치 미달 레드)에만 씁니다.

**단 하나의 예외는 결과통보서의 참고치 미달 플래그입니다(10.3).** 그 빨강은 등급이 아니라 '이 항목이 응시자 참고구간 밖'이라는 사실을 가리키며, 종합판정(A/B/C/R)에는 여전히 `--fn-error` 를 쓰지 않습니다.

---

## 3. 타이포그래피

### 3.1 서체 스택

```css
--font-sans: "Pretendard Variable", Pretendard, -apple-system, "Apple SD Gothic Neo",
             "Noto Sans KR", "Malgun Gothic", system-ui, sans-serif;
--font-mono: "JetBrains Mono", "Cascadia Mono", Consolas, "D2Coding", monospace;
```

Pretendard를 1순위로 두되 **웹폰트를 @import로 불러오지 않습니다.** 로컬 설치본을 쓰고 없으면 시스템 한글 폰트로 자연 강등합니다. 현재 프로젝트에는 `font-family` 선언 자체가 없어 브라우저 기본 고딕으로 렌더되고 있으므로, `client/src/index.css`의 `body`에 위 스택을 지정하는 것이 마이그레이션 1순위 작업입니다.

### 3.2 스케일

| 이름 | 크기 | 행간 | 자간 | 굵기 | 용도 |
|---|---|---|---|---|---|
| `display` | 32px | 1.25 | -0.02em | 700 | 보고서 지면 제목 |
| `h1` | 24px | 1.30 | -0.015em | 700 | 화면 제목 |
| `h2` | 20px | 1.35 | -0.01em | 600 | 섹션 제목 |
| `h3` | 16px | 1.40 | -0.005em | 600 | 카드 제목, 패널 제목 |
| `body` | 15px | 1.60 | 0 | 400 | 본문 |
| `body-sm` | 13px | 1.55 | 0 | 400 | 표 셀, 보조 설명 |
| `caption` | 12px | 1.45 | 0.01em | 500 | 라벨, 단위, 각주 |
| `overline` | 11px | 1.40 | 0.08em | 600 | 통계 카드 라벨 (대문자 변환은 한글에 적용 안 됨) |
| `metric-lg` | 36px | 1.00 | -0.03em | 700 | 통계 카드 주 수치 |
| `metric-md` | 28px | 1.05 | -0.025em | 700 | 표 안 강조 수치 |

### 3.3 숫자 규칙

- **모든 숫자에 `font-variant-numeric: tabular-nums`를 적용합니다.** 점수, 등급, 인원, 날짜가 표와 카드에서 세로로 정렬되어야 합니다. 전역 `body`에 걸고 예외를 두지 않습니다.
- 점수와 단위는 크기를 분리합니다. `89` 는 `metric-lg`, `점` 은 `caption` + `--text-tertiary`. 단위가 수치와 같은 크기로 커지면 수치가 안 읽힙니다.
- 등급은 `2등급`처럼 항상 단위를 붙여 표기합니다. 숫자만 두면 점수와 혼동됩니다.

### 3.4 한글 조판

- `word-break: keep-all`을 전역 적용합니다. 없으면 "강남고등학교"가 줄 끝에서 "강남고등학" / "교"로 쪼개집니다.
- 함께 `overflow-wrap: anywhere`를 걸어 긴 식별자(`올가국어_둔산점`)가 좁은 칸을 넘치지 않게 합니다.
- 본문 최대 폭 `70ch`. 보고서 지면은 `62ch`.
- 굵기는 400 / 600 / 700 세 단계만 씁니다. 500은 쓰지 않습니다(한글 폰트에서 600과 구분되지 않습니다).

---

## 4. 간격과 레이아웃

### 4.1 4px 스케일

```
--space-1: 4px    --space-5: 20px   --space-10: 40px
--space-2: 8px    --space-6: 24px   --space-12: 48px
--space-3: 12px   --space-7: 28px   --space-16: 64px
--space-4: 16px   --space-8: 32px   --space-20: 80px
```

4의 배수가 아닌 값은 쓰지 않습니다. 유일한 예외는 1px 선과 옵티컬 보정용 홀수 패딩(버튼 세로 9px 등)이며, 이 경우 주석으로 이유를 남깁니다.

### 4.2 골격 규격

| 요소 | 값 |
|---|---|
| 콘텐츠 최대 폭 | 1280px (`--container-max`) |
| 콘텐츠 좌우 패딩 | 데스크톱 32px / 태블릿 24px / 모바일 16px |
| 사이드바 폭 (펼침) | 264px |
| 사이드바 폭 (접힘) | 72px, 아이콘만 |
| 헤더 높이 | 64px (고정, 초과 금지) |
| 카드 내부 패딩 | 24px (통계 카드 20px) |
| 카드 사이 간격 | 16px |
| 섹션 사이 간격 | 32px |

### 4.3 모서리 반경

```
--radius-sm: 6px    입력, 뱃지, 작은 버튼
--radius-md: 10px   버튼, 카드
--radius-lg: 14px   모달, 큰 패널
--radius-full: 999px  아바타, 상태 점(오직 이 둘)
```

이 4단계 외의 값은 쓰지 않습니다. 특히 `rounded-2xl`(16px) `rounded-3xl`(24px)로 카드를 부풀리지 않습니다. 현재 코드에 `rounded-2xl`이 다수 있으며 전부 `--radius-md`로 내립니다.

### 4.4 표면 위계

| 층 | 배경 | 테두리 | 그림자 |
|---|---|---|---|
| 캔버스 | `--surface-sunken` | 없음 | 없음 |
| 카드 | `--surface` | `1px --border` | `--shadow-sm` |
| 팝오버/드롭다운 | `--surface-raised` | `1px --border` | `--shadow-md` |
| 모달 | `--surface-raised` | 없음 | `--shadow-lg` |

**테두리와 그림자를 둘 다 강하게 쓰지 않습니다.** 카드는 테두리로 구분하고 그림자는 거의 없게, 떠 있는 요소는 그림자로 구분합니다.

---

## 5. 컴포넌트 문법

### 5.1 버튼

| 종류 | 배경 | 글자 | 테두리 | 용도 |
|---|---|---|---|---|
| 주 (primary) | `--action` | `--action-text` | 없음 | 화면당 1개 원칙 |
| 보조 (secondary) | `--surface` | `--text-primary` | `1px --border-strong` | 취소, 부가 동작 |
| 조용한 (quiet) | 투명 | `--text-secondary` | 없음 | 표 안 인라인 동작 |
| 위험 (danger) | `--surface` | `--fn-error` | `1px --fn-error-border` | 삭제, 초기화 |
| 강조 (accent) | `--accent` | `--navy-900` | 없음 | **화면의 유일한 핵심 CTA에만.** 브라스 2곳 예산에서 1곳 차감 |

- 크기: 기본 높이 40px / 좌우 패딩 16px / `--radius-md` / `body` 크기 600 굵기.
- 작은 버튼 32px, 큰 버튼 48px. 세 가지 외 없음.
- 위험 버튼을 빨간 배경으로 만들지 않습니다. 실수 유발 대신 테두리로 경고합니다.
- `:hover`는 배경 한 단계 이동, `:active`는 `transform: scale(0.98)`. `:focus-visible`은 `outline: 2px solid var(--focus-ring); outline-offset: 2px`.
- 라벨은 한 줄. 2줄로 접히면 라벨을 줄입니다.
- **모바일(<768px)에서는 최소 44×44px.** 위 40/32/48px는 데스크톱 규격입니다. 손가락은 마우스 포인터보다 크고 정확도가 낮아, 32px 버튼은 표 안에서 오조작을 만듭니다. 데스크톱 치수를 키우는 것이 아니라 `md:` 분기로 모바일에서만 하한을 올립니다(`min-h-[44px] md:h-10 md:min-h-0`). 아이콘 버튼과 표 행 액션도 같습니다.

### 5.2 카드

**통계 카드**
```
[ 라벨 (overline, --text-tertiary) ]
[ 수치 (metric-lg) + 단위 (caption) ]
[ 각주 (caption, --text-secondary) ]
```
- 배경 `--surface`, 테두리 `1px --border`, 반경 `--radius-md`, 패딩 20px.
- 아이콘을 넣지 않습니다. 4개가 나란히 놓일 때 아이콘은 수치의 스캔을 방해합니다.
- 값이 없을 때는 `0`이 아니라 `배정 없음`처럼 상태를 문장으로 씁니다. 크기는 `h3`, 색은 `--text-tertiary`.
- 브라스를 쓰는 통계 카드는 **최대 1개**이며, 상단에 `3px --accent` 규칙선을 얹고 수치를 `--accent-strong`으로 칠합니다. 배경은 칠하지 않습니다.

**목록 카드**
- 행 사이는 `1px --border-subtle`. 카드 안 마지막 행에는 구분선을 넣지 않습니다.
- 행 전체가 클릭 가능하면 `:hover`에 `--surface-subtle` 배경만 줍니다. 이동/확대 금지.

### 5.3 뱃지

- 구조: `면(-surface) + 1px 테두리(-border) + 글자(기본색)`, 반경 `--radius-sm`, 패딩 `4px 8px`, `caption` 크기 600 굵기.
- 등급 뱃지는 2.4절 매핑을 따릅니다. 색만으로 의미를 전달하지 않도록 **숫자와 단위를 항상 함께 표기**합니다(`2등급`).
- 상태 점(`●`)을 뱃지 앞에 붙이지 않습니다.

### 5.4 표

- 머리: 배경 `--surface-subtle`, 글자 `caption` 600 `--text-secondary`, 아래 `1px --border-strong`.
- 본문 행: 아래 `1px --border-subtle`, 마지막 행 없음. **행마다 위아래 두 줄을 긋지 않습니다.**
- 세로 줄(column border) 없음. 정렬로 열을 구분합니다.
- 숫자 열은 우측 정렬 + tabular-nums, 텍스트 열은 좌측 정렬. 가운데 정렬 금지.
- 셀 패딩 `12px 16px`. 좌우 끝 셀은 바깥쪽 패딩을 0으로 두어 표가 카드 안에서 정렬되게 합니다.
- 줄무늬(zebra) 배경 금지. 행이 많으면 `:hover` 하이라이트로 해결합니다.
- 모바일에서 표는 가로 스크롤 컨테이너(`overflow-x:auto`)에 넣되, **스크롤이 생기는 표에는 첫 열을 `position: sticky`로 고정**합니다. 현재 코드는 42곳에서 `overflow-x-auto`만 쓰고 고정 열이 없어 스크롤 시 어떤 학생의 행인지 사라집니다.

### 5.5 폼 입력

- 라벨은 **항상 입력 위에** 둡니다. placeholder를 라벨로 쓰지 않습니다.
- 입력: 높이 40px(데스크톱) / **44px(모바일 <768px, 5.1과 동일한 터치 타깃 규정)**, 패딩 `0 12px`, 테두리 `1px --border-strong`, 반경 `--radius-sm`, 배경 `--surface`.
- placeholder 색은 `--text-tertiary`. 그보다 옅게 두지 않습니다.
- 도움말은 입력 아래 `caption` `--text-secondary`. 오류는 같은 자리에 `--fn-error`로 교체하고, 입력 테두리를 `--fn-error-border`로 바꿉니다.
- 오류를 색으로만 알리지 않습니다. 반드시 문장이 함께 나타납니다.
- 필수 표시는 라벨 뒤 `*`가 아니라 선택 항목에 `(선택)`을 붙이는 방식으로 합니다.

### 5.6 모달

- 배경 가림막 `rgba(19, 30, 49, 0.48)`, 패널 `--surface-raised`, 반경 `--radius-lg`, 그림자 `--shadow-lg`, 최대 폭 560px.
- 헤더는 제목(`h2`) + 닫기 버튼. 헤더 배경을 칠하지 않습니다.
- 동작 버튼은 오른쪽 아래, 순서는 `보조 → 주`.
- 열릴 때 첫 포커스는 패널이나 첫 입력으로. `Esc`로 닫힙니다.

### 5.7 토스트

- 우측 하단, 폭 360px, `--surface-raised`, `1px --border`, `--shadow-md`.
- 상태는 왼쪽 `3px` 세로 규칙선으로만 표현합니다(`--fn-success` / `--fn-error` / `--fn-info`). 패널 전체를 상태색으로 칠하지 않습니다.
- 성공 3초, 오류는 자동으로 사라지지 않고 닫기 버튼을 둡니다.

---

## 6. 야간 모드

### 6.1 전략

`[data-theme="dark"]`를 문서 루트에 붙여 토큰만 교체합니다. 컴포넌트 CSS는 라이트와 완전히 동일하며, 다크 전용 클래스를 만들지 않습니다.

현재 프로젝트는 `tailwind.config.js`가 `darkMode: ['class']`, `client/src/index.css`가 `.dark` 선택자를 쓰고 있습니다. 마이그레이션 기간에는 두 선택자를 함께 지정합니다.

```css
[data-theme="dark"], .dark { /* 토큰 재정의 */ }
```

`prefers-color-scheme`으로 자동 전환하지 않습니다. 성적 화면은 상담 중 함께 보는 경우가 많아, 사용자가 명시적으로 고른 모드가 유지되어야 합니다.

### 6.2 다크 토큰

```css
[data-theme="dark"] {
  /* 표면 사다리: 캔버스가 가장 어둡고 위로 올라올수록 밝아진다 */
  --surface:          var(--navy-800);   /* 카드 */
  --surface-sunken:   var(--navy-950);   /* 캔버스 */
  --surface-raised:   var(--navy-700);   /* 모달, 팝오버 */
  --surface-inverse:  var(--navy-900);   /* 헤더, 사이드바 */
  --surface-subtle:   var(--navy-700);

  --text-primary:     #EEF2F8;
  --text-secondary:   var(--navy-200);
  --text-tertiary:    var(--navy-300);
  --text-on-inverse:  #EEF2F8;
  --text-on-inverse-muted: var(--navy-300);
  --text-accent:      var(--brass-400);

  --border:           var(--navy-600);
  --border-strong:    var(--navy-500);
  --border-subtle:    var(--navy-700);
  --border-inverse:   var(--navy-700);

  --action:           var(--navy-100);
  --action-hover:     #FFFFFF;
  --action-active:    var(--navy-200);
  --action-text:      var(--navy-900);
  --action-subtle:    var(--navy-700);
  --action-subtle-hover: var(--navy-600);
  --focus-ring:       var(--navy-300);

  --accent:           var(--brass-400);
  --accent-strong:    var(--brass-300);
  --accent-surface:   #2A2314;

  --fn-success:         #4FBF85;
  --fn-success-surface: #12301F;
  --fn-success-border:  #2C6E4A;
  --fn-warning:         #D9A441;
  --fn-warning-surface: #33270D;
  --fn-warning-border:  #7A5C1E;
  --fn-error:           #F0837C;
  --fn-error-surface:   #35191A;
  --fn-error-border:    #7E3B39;
  --fn-info:            #4FC2CF;
  --fn-info-surface:    #102E33;
  --fn-info-border:     #2A6068;

  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.4);
  --shadow-md: 0 2px 8px rgba(0, 0, 0, 0.5);
  --shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.6);
}
```

**다크에서 뒤집히는 것:** 주 버튼은 밝은 면 + 어두운 글자가 됩니다(어두운 배경 위 어두운 버튼은 사라지므로). 라이트에서 헤더는 캔버스보다 어두웠지만, 다크에서는 표면 사다리를 따라 캔버스(`--navy-950`)보다 한 단계 밝은 `--navy-900`이 됩니다. 다크에서 헤더를 캔버스와 같은 값으로 두면 구조가 배경에 녹아 사라지므로, "더 어둡게"가 아니라 **"한 단계 위로"**가 규칙입니다.

**다크에서 유지되는 것:** 브라스는 여전히 화면당 2곳입니다. 어두운 배경에서 브라스가 더 잘 보이므로 늘리고 싶어지지만, 늘리면 이원화가 무너집니다.

### 6.3 적용 대상

| 화면 | 야간 모드 |
|---|---|
| 지점 대시보드, 전체관리자 대시보드 | **필수.** 야간 근무가 실제로 발생하는 화면 |
| 학생 대시보드 | 필수 |
| 로그인 | 필수 |
| 내부 업무 보고서 화면 (강사가 화면으로 보는 성취 보고서) | 필수. 기준 구현 `reference/work-report-mockup.html`이 이 경우 |
| 학부모 배포본 보고서 (인쇄/PDF로 내보내는 최종본) | **제공하지 않음.** 배포 산출물이므로 라이트 고정, 10.3절 인쇄 규칙이 지배 |

---

## 7. 반응형

### 7.1 브레이크포인트

```
sm   640px    큰 휴대폰 가로
md   768px    태블릿 세로 / 데스크톱 레이아웃 시작점
lg  1024px    노트북
xl  1280px    콘텐츠 최대 폭 도달
```

### 7.2 사이드바: 모바일에서 오버레이 드로어로 전환 (강제)

**이행 완료.** 아래는 이행 전 상태의 기록입니다. 당시 세 대시보드의 `<aside>`는 폭만 바꾸는 인라인 요소였습니다.

- `client/src/pages/AdminDashboard.tsx:1334` : `sidebarOpen ? 'w-64' : 'w-20'`
- `client/src/pages/BranchDashboard.tsx:1895` : `sidebarOpen ? 'w-64' : 'w-20'`
- `client/src/pages/StudentDashboard.tsx:988` : `sidebarOpen ? 'w-72' : 'w-0'`

세 곳 모두 `md:` 계열 분기가 없습니다. 그 결과 375px 화면에서 사이드바가 문서 흐름 안에 남아 본문 폭을 264~288px 잠식하고, 본문은 90px 남짓한 칸에 갇혀 표와 카드가 무너집니다. 이것이 현재 앱의 모바일 붕괴 원인입니다.

**규정.**

| 뷰포트 | 사이드바 동작 |
|---|---|
| `>= 768px` | 문서 흐름 안 고정 기둥. 펼침 264px / 접힘 72px |
| `< 768px` | **흐름에서 완전히 제거하고 오버레이 드로어로 전환** |

`< 768px`에서 지켜야 할 것:

1. 사이드바는 `position: fixed; inset-block: 0; left: 0; width: 264px; z-index: 40`, 닫힌 상태는 `transform: translateX(-100%)`.
2. 본문은 사이드바 폭에 대한 `margin-left`를 갖지 않습니다. 항상 `width: 100%`.
3. 열릴 때 `rgba(19,30,49,0.48)` 가림막을 깔고, 가림막 클릭과 `Esc`로 닫힙니다.
4. 열린 동안 `body`에 스크롤 잠금.
5. 헤더 왼쪽에 메뉴 버튼(44x44px 이상)을 노출합니다.
6. 드로어 전환은 `transform` 200ms만. 폭(`width`) 애니메이션은 레이아웃을 재계산하므로 금지.

### 7.3 그 밖의 붕괴 규칙

- 통계 카드 4열 → `md` 2열 → `sm` 1열.
- 2단 패널(차트 + 분포) → `md` 미만 세로 스택.
- 표는 5.4절대로 가로 스크롤 + 첫 열 고정.
- 터치 타깃 최소 44x44px. 표 안 인라인 버튼도 예외 없습니다.
- 모바일에서 어떤 화면도 가로 스크롤이 생기면 안 됩니다(`document.documentElement.scrollWidth <= viewport`). 이것은 릴리스 게이트입니다.

---

## 8. 모션

강도는 3단계 중 낮은 쪽에 고정합니다. **성적 화면에서 움직임은 정보를 방해합니다.**

### 8.1 허용

| 대상 | 속성 | 시간 | 이징 |
|---|---|---|---|
| hover 상태 | `background-color`, `border-color`, `color` | 150ms | `ease-out` |
| active 눌림 | `transform: scale(0.98)` | 100ms | `ease-out` |
| 드로어/모달 열림 | `transform`, `opacity` | 200ms | `cubic-bezier(0.32, 0.72, 0, 1)` |
| 토스트 등장 | `transform`, `opacity` | 200ms | 동일 |

### 8.2 금지

- 자동 재생 애니메이션 전부(펄스, 시머, 무한 루프, 카운트업 숫자).
- 스크롤 연동 등장 애니메이션. 데이터는 즉시 읽혀야 합니다.
- `width`, `height`, `top`, `left` 애니메이션. `transform`과 `opacity`만.
- 200ms를 넘는 UI 전환.
- 차트 그리기 애니메이션. SVG는 완성된 상태로 렌더합니다.

### 8.3 감소 모션

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

자동 애니메이션이 애초에 없으므로 이 블록은 hover 전이만 제거합니다. 그래도 명시적으로 넣습니다.

**예외 — 로딩 표시는 허용합니다.** 데이터를 기다리는 동안의 스켈레톤 펄스와 스피너는 8.2의 자동 애니메이션 금지 대상이 아닙니다. 값을 못 받아온 상태를 `0`으로 그리면 "실제로 0"과 구분되지 않아 사용자가 잘못 읽습니다. 로딩·실패·성공은 서로 다른 표시를 가져야 하며, 이때의 최소한의 움직임은 정보입니다. 감소 모션 설정에서는 위 블록이 이 펄스도 함께 멈춥니다.

---

## 9. 마이그레이션 매핑표

> **이 장은 이행 완료 기록입니다.** 아래 표의 "현재" 열은 마이그레이션 착수 시점의 상태이며, 모두 "대체" 열대로 이행되었습니다(커밋 `ce88853`, `0040f3e`). 새로 작성하는 화면은 대체 열만 따르면 되고, 현재 열은 과거 코드를 읽을 때의 참조로만 씁니다. 여기 언급된 백업 파일(`*.backup`, `*_old.tsx`)은 커밋 `dd9575a` 에서 삭제되어 더 이상 없습니다.

`client/src/pages/*.tsx` 실사용 클래스를 조사해 작성했습니다. 조사 시점 현황: 그라디언트 사용 `bg-gradient-to-*` **118곳**(로그인 4, 전체관리자 26, 지점 30, 학생 58), 원시 hex는 `#fff` 4곳뿐이고 나머지는 전부 Tailwind 팔레트 유틸리티입니다.

### 9.1 전역 우선 규칙

| 현재 | 대체 | 비고 |
|---|---|---|
| `bg-gradient-to-r`, `bg-gradient-to-br`, `bg-gradient-to-b` (118곳) | 단색 `bg-[var(--surface-inverse)]` 또는 `bg-[var(--surface)]` | 그라디언트 전면 폐지. `via-*` 중간 정지점은 그냥 삭제 |
| `bg-clip-text text-transparent` (로고 텍스트) | `text-[var(--text-primary)]` | 텍스트 그라디언트 폐지 |
| `rounded-2xl`, `rounded-3xl` | `rounded-[var(--radius-md)]` | 10px로 통일 |
| `shadow-xl`, `shadow-2xl` | `shadow-[var(--shadow-md)]` | 카드에는 `--shadow-sm` |
| `shadow-blue-500` (BranchDashboard) | `shadow-[var(--shadow-md)]` | 컬러 그림자 폐지 |
| `backdrop-blur-md` (헤더 3곳) | 제거, 불투명 `--surface-inverse` | 스크롤 중 GPU 재도색 비용 |
| 폰트 선언 없음 (`tailwind.config.js`에 `fontFamily` 미설정) | `body { font-family: var(--font-sans) }` | 3.1절 |

### 9.2 페이지 아이덴티티 색 통합

**LoginPage + AdminDashboard (현재 오렌지레드 계열)**

| 현재 | 대체 |
|---|---|
| `bg-gradient-to-br from-orange-400 via-red-500 to-pink-500` (로그인 배경, L34) | `bg-[var(--surface-sunken)]` |
| `bg-gradient-to-r from-orange-500 to-red-600` (주 버튼, 5곳) | `bg-[var(--action)] text-[var(--action-text)]` |
| `bg-gradient-to-br from-orange-500 to-red-600` (로고 타일, 5곳) | `bg-[var(--surface-inverse)]` |
| `hover:from-orange-600 hover:to-red-700` | `hover:bg-[var(--action-hover)]` |
| `from-orange-50 to-red-50` (옅은 면, 7곳) | `bg-[var(--surface-subtle)]` |
| `border-orange-100` (사이드바/헤더 경계, 4곳) | `border-[var(--border)]` |
| `border-orange-500`, `ring-orange-500` (입력 포커스, 각 3곳) | `outline-[var(--focus-ring)]` |
| `text-orange-600`, `text-orange-500` | `text-[var(--text-secondary)]` |
| `bg-white/80` (헤더 L1403) | `bg-[var(--surface-inverse)]` + 흰 글자 |

**BranchDashboard (현재 인디고블루 계열)**

| 현재 | 대체 |
|---|---|
| `bg-gradient-to-r from-blue-500 to-indigo-600` (주 버튼, 5곳) | `bg-[var(--action)]` |
| `bg-gradient-to-r from-blue-50 to-indigo-50` (패널 머리, 7곳) | `bg-[var(--surface-subtle)]` |
| `bg-gray-900` (사이드바 L1895) | `bg-[var(--surface-inverse)]` |
| `border-gray-800` (사이드바 경계) | `border-[var(--border-inverse)]` |
| `text-indigo-600`, `bg-indigo-50`, `border-indigo-300` | `--text-secondary` / `--surface-subtle` / `--border-strong` |
| `bg-gradient-to-r from-emerald-50 to-teal-50` | `bg-[var(--fn-success-surface)]` (실제 성공 의미일 때만) |
| `text-teal-600`, `ring-teal-300`, `bg-teal-50` | `--fn-info` 계열 |
| `border-blue-100` (헤더 L1964) | `border-[var(--border-inverse)]` |

**StudentDashboard (현재 퍼플 계열, 그라디언트 58곳으로 최다)**

| 현재 | 대체 |
|---|---|
| `bg-gradient-to-br from-purple-600 via-purple-700 to-indigo-800` (사이드바 L988) | `bg-[var(--surface-inverse)]` |
| `bg-gradient-to-r from-purple-600 via-purple-700 to-indigo-700` (히어로 패널, 2곳) | `bg-[var(--surface-inverse)]` |
| `bg-gradient-to-r from-purple-600 to-indigo-600` (주 버튼/패널 머리, 4곳) | `bg-[var(--action)]` |
| `bg-gradient-to-br from-purple-500 to-indigo-600` (아바타 타일, 4곳) | `bg-[var(--surface-inverse)]` |
| `bg-gradient-to-br from-red-500 to-pink-600` (통계 아이콘 타일, 3곳) | 타일 자체 삭제 (5.2절: 통계 카드에 아이콘 없음) |
| `bg-gradient-to-br from-green-400 to-emerald-500` (성취 원, 3곳) | `bg-[var(--fn-success-surface)]` + `text-[var(--fn-success)]` |
| `bg-gradient-to-r from-orange-500 to-amber-500` (최고 기록 강조, 2곳) | **`--accent` 계열.** 브라스 2곳 예산의 1곳 |
| `text-purple-600` (15곳), `text-purple-200` (13곳) | `--text-secondary` / `--text-on-inverse-muted` |
| `bg-purple-50`, `border-purple-100~500` | `--surface-subtle` / `--border` |
| `text-indigo-200` (등급 라벨 L1777) | `--text-on-inverse-muted` |

### 9.3 중립 회색 → 시맨틱 토큰

| 현재 | 대체 | 사용량 |
|---|---|---|
| `text-gray-900`, `text-gray-800` | `text-[var(--text-primary)]` | 61곳 |
| `text-gray-700` | `text-[var(--text-primary)]` | 155곳 (최다) |
| `text-gray-600`, `text-gray-500` | `text-[var(--text-secondary)]` | 82곳 |
| `text-gray-400`, `text-gray-300` | `text-[var(--text-tertiary)]` | 34곳 |
| `border-gray-100` | `border-[var(--border-subtle)]` | 30곳 |
| `border-gray-200`, `border-gray-300` | `border-[var(--border)]` | 27곳 |
| `bg-gray-50` | `bg-[var(--surface-sunken)]` | 18곳 |
| `bg-gray-100`, `bg-gray-200` | `bg-[var(--surface-subtle)]` | 10곳 |
| `bg-gray-900`, `bg-gray-800` | `bg-[var(--surface-inverse)]` | 3곳 |
| `divide-gray-200` | `divide-[var(--border-subtle)]` | 1곳 |

### 9.4 상태색 → 기능 계층

| 현재 | 대체 | 조건 |
|---|---|---|
| `text-green-600`, `text-green-700`, `text-emerald-600` | `text-[var(--fn-success)]` | 실제 성공/상승 의미일 때만 |
| `bg-green-50`, `bg-emerald-50`, `bg-green-100` | `bg-[var(--fn-success-surface)]` | |
| `border-green-200`, `border-green-300`, `border-green-400` | `border-[var(--fn-success-border)]` | |
| `text-red-600`, `text-red-700`, `text-red-500` | `text-[var(--fn-error)]` | 시스템 오류/삭제만. 낮은 등급에는 금지(2.4절) |
| `bg-red-50`, `bg-red-100` | `bg-[var(--fn-error-surface)]` | |
| `bg-red-500`, `bg-red-600` (지점 대시보드 실면) | `bg-[var(--surface)]` + `text-[var(--fn-error)]` + `border-[var(--fn-error-border)]` | 5.1절 위험 버튼 |
| `text-yellow-700/800`, `bg-yellow-50/100`, `border-yellow-200` | `--fn-warning` 계열 | |
| `text-blue-600`, `bg-blue-50`, `border-blue-200/300` | 구조면 `--surface-subtle`, 정보면 `--fn-info` 계열 | **사용처를 하나씩 판별해야 함.** 현재 파랑이 구조와 정보 양쪽에 섞여 있음 |
| `text-orange-700`, `bg-orange-100` (학생 화면 상태 표시) | `--fn-warning` 계열 | |

### 9.5 구조적 결손 (색 치환이 아닌 신규 작업)

| 항목 | 현재 | 조치 |
|---|---|---|
| 등급 뱃지 | 헬퍼 함수 없음. `StudentDashboard.tsx:1778`에서 `{grade}등급`을 `text-2xl font-bold` 평문으로 출력 | 2.4절 매핑을 구현한 `<GradeBadge grade={n} />` 신설 |
| 사이드바 모바일 | `md:` 분기 없음 (3개 파일) | 7.2절 드로어로 교체 |
| 표 첫 열 고정 | `overflow-x-auto` 42곳, sticky 열 0곳 | 5.4절 적용 |
| 폰트 | 선언 없음 | 3.1절 스택을 `index.css`에 지정 |
| 다크 토큰 | `index.css`에 shadcn 기본값(`--primary: 210 100% 50%`)만 있고 화면이 참조하지 않음 | 2장, 6.2절 토큰으로 교체 |
| 숫자 정렬 | `tabular-nums` 미적용 | 3.3절 전역 적용 |
| `#fff` 리터럴 4곳 | 하드코딩 | `var(--surface)` 또는 `var(--text-on-inverse)` |

**권장 순서:** 9.1 전역 규칙 → 9.3 중립 회색(기계적 치환, 위험 낮음) → 9.2 페이지별 아이덴티티 → 9.4 상태색(판별 필요) → 9.5 구조 작업.

---

## 10. 보고서 지면 문법

학부모에게 전달되는 성취 보고서 화면 전용 규칙입니다. 앱 UI가 아니라 **문서**로 취급합니다.

### 10.1 톤 (v4 개정: 데이터 리포트에서 결과통보서로)

**개정 이력.** v1은 절제된 에디토리얼 인쇄물, v2는 색을 입힌 인쇄물, v3는 대시보드 문법의 데이터 리포트였습니다. 셋 다 "보기 좋은 성적표"였지만, 학부모가 이 문서를 받아 들고 **무엇을 어떻게 해야 하는지**를 곧바로 알기는 어려웠습니다. v4에서 참조 대상을 바꿉니다. 지향점은 대시보드가 아니라 **병원 건강검진 결과통보서**입니다.

문서 이름: **학습 성취 분석 결과통보서**, 부제는 **미리 보는 수능 등급 검사**. 학생은 수검자, 시험은 검사, 영역과 난이도는 검사항목, 응시자 집단의 분포는 참고치입니다.

**정체성(v5).** '미수등'은 '미리 보는 수능 등급'입니다. 이 문서는 지금 학년의 성적표가 아니라 **지금 실력으로 수능 국어를 치르면 어느 등급대에 놓이는지 미리 진단하는 검사지**입니다. 표지 부제, 러닝헤드, 예상 수능 등급 블록, 고교 진학 대비 소견이 이 정체성을 지면 위에서 반복해 확인시킵니다. 소견 문장도 "이번 시험을 잘 봤는가"가 아니라 "수능 기준으로 지금 어디에 서 있고 무엇이 발목을 잡는가"에 답해야 합니다.

- **결론이 첫 장에 판정으로 나옵니다.** 검진지의 '정상A / 정상B / 질환의심'처럼, 등급을 판정 코드(A/B/C/R)와 판정문으로 먼저 말합니다. 수치는 판정의 근거로 뒤따릅니다.
- **모든 결과는 참고치와 나란히 놓입니다.** 절대 수치만 적힌 칸은 만들지 않습니다. `결과 | 참고치 | 판정` 세 칸이 한 줄에 있어야 그 수치의 의미가 성립합니다.
- **이상 항목만 색과 기호로 튀어나옵니다.** 정상 항목은 무채색으로 눕히고, 참고치를 벗어난 항목에만 플래그를 답니다. 전 항목을 다 강조하면 아무것도 강조되지 않습니다.
- **임상적 절제.** 장식색, 그라디언트, 아이콘 장식, 카드 그림자 놀이를 쓰지 않습니다. 흰 종이 위의 괘선과 표가 기본 문법입니다. 색은 정보일 때만 씁니다.
- **처방으로 끝납니다.** 마지막은 감상이 아니라 번호 매긴 권고사항과 재검사 시점입니다.
- 문장체를 씁니다. "화법 20%"가 아니라 "화법 영역이 참고치 40 ~ 100% 아래인 20%입니다."

**v3에서 폐기하는 것:** 히어로 그라디언트 밴드, 골드 대형 등급 수치, 옅은 청회색 캔버스 위의 라운드 카드, 카드 헤더의 원형 액센트. v4의 지면은 흰 서식지입니다.

### 10.2 결과통보서 지면 구성 (v4)

| 요소 | 규격 |
|---|---|
| 캔버스 | `--paper-bg` 중성 회청색. 지면은 흰 면 + `--form-line` 1px 테두리, 반경 0 (서식지는 둥글지 않습니다) |
| 표지 문서 헤더 | 네이비 단색 밴드(`--surface-inverse`). 기관명 9.5px/700 + 문서명 27px/800 + 영문 부제 + 우측 발행 정보 3행 |
| 수검자 정보표 | `border-collapse` 표. 라벨 칸은 `--form-head` 면 + 9.5px/700, 값 칸은 11.5px/600 |
| 종합판정 박스 | 좌측 판정 코드 칸(40px/800, 판정 색 면) + 우측 등급·판정문. 하단에 등급 게이지 하나 (A/B/C/R 구간을 게이지 머리에 얹어 표기) |
| 검사표 | `검사항목 \| 결과 \| 참고치 \| 판정 \| 상세` 5열 고정. 결과 12.5px/800, 참고치 10px, 판정은 플래그 뱃지, 상세는 쪽 번호 |
| 참고범위 밴드 | 16px 높이 눈금에 4구역(미달/주의/정상/우수)을 칠하고 수검자 위치를 2px 마커로. 수치 뱃지는 눈금 **위**에 띄웁니다 (눈금 안에 두면 구역을 가립니다) |
| 소견 블록 | 좌측 3px 네이비 바 + 결론 12.5px/700 + 근거 9.5px |
| 이상 항목 상자 | `--fn-error` 계열 1px + 좌측 4px. 정상일 때만 `--fn-success` 계열 |
| 권고사항 | 번호 사각 뱃지 + 태그 + 제목 11.5px/800 + 본문 10.5px. 처방전처럼 위에서부터 처치 순서 |
| 러닝헤드 | 좌측 문서명 8.5px + 지면 제목 19px/800, 우측 수검자명 + 알약형 `n / 총쪽` 뱃지 |
| 지면 꼬리 | 기관명 / 문서번호 / 쪽 번호 3분할 |

**지면 높이는 1123px 고정입니다.** PDF가 A4 한 장에 한 지면을 그대로 얹기 때문에, 넘치면 잘리는 게 아니라 찌그러집니다. 새 내용을 넣을 때는 `.page-body` 의 `scrollHeight - clientHeight` 가 0인지 반드시 확인합니다.

#### 10.2.1 지면 순서 (v5, 8쪽)

문항 데이터가 있는 정상 발행본 기준입니다. 고교 진학 대비 소견은 데이터가 없으면 지면째 빠지고 목차에서도 사라집니다(빈 지면을 내지 않습니다).

| 쪽 | 지면 | 싣는 것 |
|---|---|---|
| 1 | 표지 · 종합 판정 | 문서 헤더(부제 "미리 보는 수능 등급 검사"), 수검자 정보표, 종합 판정 + 등급 게이지, **수능 국어 예상 등급 구간**, 종합 수치 4칸, 경과 관찰(직전 검사 대비) 표, 고지문 |
| 2 | 검사 항목별 판정 요약 | 종합·영역별·난이도별 검사표, 이상 항목 상자, 상세 분석 지면 안내, 판정 기호 범례 |
| 3 | 참고범위 대비 성취 분포 | 종합 성취 수준, 영역별·난이도별 성취 분포 밴드, 구역 범례 |
| 4 | 검사 구성 및 출제 경향 분석 | 검사 구성 개요 4칸, 영역별 배점 및 난이도 분포, 고난도 문항 출제 경향, 출제자 총평 |
| 5 | 문항별 정오 분석 | 전 문항 정오 분포 히트맵, **주요 오답 문항 정밀 분석**(최대 6문항), 원자료는 상담 제공 안내 |
| 6 | 종합 진단 소견 | 판정 요약, 총평, 영역별 진단 소견(영역마다 3~5문장), 학습 성향 유형 |
| 7 | 상급 과정 진학 대비 종합 소견 | 진학 후 전망(4~6문장), 우선 준비 영역 2~3개(데이터 근거 + 번호 매긴 행동 2개 이상), 고1 첫 학기 준비 점검 3~4항목, 맺음말 |
| 8 | 학습 처방 및 관리 권고 | 번호 처방 목록 7갈래(영역 교정 / 유형 교정 / 실점 회복 / 학습 설계 12주 / 경과 관찰 / 강점 유지 / 가정 협조), 경과 관찰 및 재검사 계획 |

**v4c(9쪽)에서 없앤 것:** 정답률 도판 지면(문항별 정답률·난이도 비교·레이더·점수 분포) — 난이도 수치는 2쪽 검사표에, 분포상 위치는 1쪽 석차에 이미 있습니다. 문항별 45행 검사표와 전체 오답 해설 2쪽 — 정오는 히트맵이, 해설은 핵심 오답 6문항이 대신합니다.

이 중 **응시자 점수 분포 곡선만은 대체 표현이 없습니다.** 압축을 우선하기로 하여 v5에서는 넣지 않고, 응시자 표본이 충분히 쌓이면 3쪽 참고범위 밴드 지면 하단에 소형으로 재검토합니다(2026-08-21 결정, 보류).

#### 10.2.1b 용어 규약 (v5b)

이 문서는 학원 안내문이 아니라 **진단 결과통보서**입니다. 지면 제목·표 머리·본문은 교육 진단 용어를 씁니다. 다만 검진 결과지의 격식 수준을 넘는 현학은 금지입니다 - 학부모가 사전 없이 읽을 수 있어야 합니다.

| 쓰지 않는 말 | 쓰는 말 |
|---|---|
| 결과 (표 머리) | 측정값 |
| 검사 결과 요약 | 검사 항목별 판정 요약 |
| 위치 / 밴드 | 성취 분포 / 성취 수준 |
| 정오 현황 | 문항별 정오 분석 · 전 문항 정오 분포 |
| 핵심 오답 분석 | 주요 오답 문항 정밀 분석 |
| 항목별 소견 | 영역별 진단 소견 |
| 고교 진학 대비 소견 | 상급 과정 진학 대비 종합 소견 |
| 권고사항 | 학습 처방 및 관리 권고 |
| 이전 검사 / 이번 검사 | 직전 검사 / 금회 검사 |
| 추이 | 경과 관찰 |
| 아쉬운 실점 | 기준 대비 실점 |
| 문항별 상세 데이터 | 원자료 |
| 약점 | 취약 요인 |
| 처치 / 공략 | 교정 전략 |

**본문도 같이 옮깁니다.** 제목만 격상하고 본문이 구어로 남으면 문서의 격이 갈라집니다.

#### 10.2.2 같은 정보를 두 번 그리지 않습니다 (강제)

지면이 늘어나는 가장 흔한 이유는 정보가 늘어서가 아니라 **같은 정보를 형태만 바꿔 반복**하기 때문입니다. 아래는 v4 압축 패스에서 실제로 걷어낸 반복이며, 같은 종류의 반복을 다시 만들지 않습니다.

| 반복 | 처리 | 단일 진실 |
|---|---|---|
| 영역별 성취가 요약표·검사표·상세 카드·성취 바·레이더에 5회 | 성취 바 삭제, 상세 카드는 서술만 남겨 종합소견으로 통합 | 2쪽 검사표 (결과·참고치·판정) |
| 등급이 판정 박스·A/B/C/R 척도·1~9 게이지에 3회 | 척도행 삭제, 구간을 게이지 머리에 통합 | 1쪽 등급 게이지 |
| 변별(아쉬운 실점·강점 근거)이 별도 목록으로 한 지면 | 문항표 행 표시(`!` / `+`)로 흡수, 지면 삭제 | 5쪽 문항별 검사표 |
| 강점·보완 카드와 영역별 소견이 같은 영역을 두 번 서술 | 항목별 소견 하나로 합치고 강점/보완은 태그로 표시 | 8쪽 항목별 소견 |
| 12주 계획이 처치 일정표·목표 정답률 차트·장기 관리 단계표에 3회 | 처방 목록의 마지막 항목으로 흡수, 표·차트 삭제 | 9쪽 권고사항 |

**규칙:** 어떤 수치를 두 번째로 그리려 할 때는, 두 번째 표현이 *첫 번째가 답하지 못하는 질문*에 답하는지 먼저 확인합니다. 검사표는 "얼마인가"에, 밴드는 "어디쯤인가"에 답하므로 둘 다 남습니다. 성취 바는 밴드와 같은 질문에 답하므로 지웠습니다.

### 10.3 참고치와 판정 (데이터 규칙)

이 절은 미학이 아니라 **데이터 정직성** 규칙입니다.

- **참고치는 지어내지 않습니다.** 같은 검사를 제출한 응시자들의 항목별 정답률 분포에서 **제10 ~ 제90 백분위**를 참고구간으로 씁니다. 임상 검사의 참고구간과 같은 방식입니다.
  - 평균 ± 표준편차는 쓰지 않습니다. 0/100 에서 잘려 상한이 100%로 붙어버리고, 그러면 `우수` 판정이 영영 성립하지 않습니다.
  - 사분위(제25 ~ 제75) 구간도 쓰지 않습니다. 응시자 절반이 미달로 찍혀 플래그의 의미가 희석됩니다.
- **표본이 5명 미만이면 구간을 만들지 않고 `기준 축적 중`으로 적습니다.** 이때 판정 칸은 점선 테두리의 중립 뱃지가 되고, 결과는 절대 정답률로만 읽습니다.
- **판정 매핑 (등급 -> 판정).** 등급 색은 2.4를 그대로 따릅니다.

| 등급 | 판정 | 코드 | 토큰 |
|---|---|---|---|
| 1 ~ 2 | 우수 | A | `--fn-success` 계열 |
| 3 ~ 4 | 양호 | B | `--fn-info` 계열 |
| 5 ~ 6 | 보완 필요 | C | `--surface-subtle` + `--text-secondary` (무채색) |
| 7 ~ 9 | 집중 관리 | R | `--fn-warning` 계열 |

- **플래그 (참고치 대비).** 항목 단위 판정입니다.

| 조건 | 표기 | 토큰 |
|---|---|---|
| 결과 > 참고치 상한 | `▲ 우수` | `--fn-success` |
| 참고치 안 | `정상` | `--text-secondary` (무채색) |
| 결과 < 참고치 하한 | `▼ 미달` | `--fn-error` |
| 표본 부족 | `기준 축적 중` | `--text-tertiary`, 점선 |

  **2.4의 `--fn-error` 금지 조항에 대한 예외입니다.** 2.4가 막는 것은 *등급 표기*에 빨강을 쓰는 일입니다. 여기 빨강은 등급이 아니라 **한 항목이 참고구간 밖에 있다는 사실**을 가리키며, 검진지에서 L 플래그가 파랑/빨강으로 표시되는 것과 같은 역할입니다. 종합판정(A/B/C/R)에는 여전히 `--fn-error` 를 쓰지 않습니다.

- **예상 수능 등급은 예언이 아니라 옮겨 적기입니다.** 이번 검사에서 *측정된* 등급을 수능 척도 위에 `측정등급 -1 ~ 측정등급 +1` 구간으로 적습니다. 새 점수를 계산하거나 미래를 추정하지 않습니다. 표기 규칙 셋을 모두 지킵니다.
  1. **구간으로 씁니다.** "4등급입니다"가 아니라 "3 ~ 5등급대에 놓입니다".
  2. **조건을 답니다.** "현 성취를 그대로 유지할 경우".
  3. **각주를 답니다.** "현 시점 진단이며 확정 예측이 아닙니다"와 근거 등급·응시자 수를 함께 적습니다.
  등급이 없으면 블록을 만들지 않습니다.
- **AI 소견 필드가 없으면 지면을 비우지 않고 빼거나, 실데이터로 만든 대체본을 씁니다.** 대체본을 썼을 때는 지면에 그 사실을 밝힙니다("이번 채점 결과에서 직접 산출한 항목이며, 지도 교사 소견은 상담 시 별도 제공"). 대체본의 문장에 들어가는 영역명·문항수·배점·정답률·참고치·오답 소분류는 전부 실제 채점 결과에서 나와야 합니다.
- **학습 처방(9쪽)은 실데이터 조립본이 정식 경로이며, AI 값이 오면 우선 사용합니다.** 프롬프트 v3는 `analysis.prescription`을 계속 요구하고 그 값이 오면 조립본보다 먼저 씁니다. 다만 실측에서 이 필드는 반복적으로 누락됐고(3회 시도, 3회 모두 빈 값), 처방은 "무엇을 얼마나 틀렸는가"에서 곧장 나오는 섹션이라 **검증 가능한 실계산 수치가 AI 서술보다 적합**합니다. 그래서 조립본을 예외적 대체가 아니라 **기본 경로**로 둡니다 (2026-08-21 결정).
- **AI 텍스트 길이는 보장되지 않으므로 조판이 상한을 강제합니다.** 프롬프트가 영역당 300 ~ 420자를 요구해도 그보다 길게 오는 경우가 있고(실측 537 ~ 665자), 그대로 얹으면 지면이 넘칩니다. 지면을 늘리거나 활자를 더 줄이는 대신 `OPINION_MAX`로 문장 경계에서 자르고 "(이하 생략)"을 표시합니다. **2열 조판은 해법이 아닙니다** - 폭이 절반이 되면 줄 수가 두 배가 되어 오히려 높이가 늘어납니다(실측: 넘침 214px에서 281px로 악화).
- **추이는 실제 이전 응시 기록하고만 비교합니다.** 이전 기록이 없으면 "이번 결과가 최초 기준선"이라고 적습니다. 시험이 다르면 만점이 다르므로 정답률(%)로 비교하고 검사명을 함께 적습니다.
- **예측선을 예측이라고 부르지 않습니다.** 현재 값에서 기계적으로 올린 선은 `목표 정답률`이며, 캡션에 "예측된 성적이 아니라 관리 목표"라고 명시합니다.
- **응시자 분포 도판은 실제 점수로만 그립니다.** 표본이 5명 미만이면 도판을 그리지 않고 그 사실을 적습니다.

### 10.4 요약 지면은 목차입니다

1쪽이 판정, 2쪽이 요약입니다. 요약 지면은 전 항목 검사표 + 이상 항목 하이라이트 + 상세 쪽 번호 참조로 구성하며, **뒷장을 펴지 않아도 무엇이 문제인지 끝나야 합니다.**

- 검사표의 마지막 열은 항상 `상세` 쪽 번호입니다. 쪽 번호는 조립 순서에서 자동으로 나오고, 본문에서는 자리표시자로 적었다가 마지막에 치환합니다. 손으로 적은 쪽 번호를 두지 않습니다.
- 요약 지면 하단에는 **판정 기호 안내**를 둡니다. 검진지가 판정구분표를 싣는 것과 같은 이유이며, 기호를 처음 보는 학부모가 되묻지 않게 합니다.
- 이상 항목 상자에는 반드시 수치와 참고치를 함께 적습니다. "화법 보완 필요"가 아니라 "화법 20% (참고치 40 ~ 100%)".

### 10.5 타이포그래피

Pretendard 단일 위계입니다. 세리프는 쓰지 않습니다(v3에서 폐지, v4에서도 유지).

| 대상 | 규격 |
|---|---|
| 문서명 (표지) | 27px / 800 / `-0.045em` |
| 지면 제목 (러닝헤드) | 19px / 800 / `-0.03em` |
| 판정 코드 | 40px / 800 / `-0.05em` |
| 등급 수치 | 34px / 800 / `-0.05em` / 브라스 |
| 섹션 제목 | 11px / 800 / `+0.06em` + 좌측 3px 네이비 바 |
| 검사표 결과 | 12.5px / 800 |
| 검사표 본문 | 10.5px / 400 ~ 700 |
| 소견 결론 | 12.5px / 700 |
| 본문 / 소견 문장 | 10.5px / 400 ~ 500, 행간 1.85 ~ 2.0 |
| 캡션 / 고지문 | 8.5 ~ 9px |

모든 수치에 `tabular-nums` 를 유지합니다. 브라스는 지면당 2곳 이하이며, 표지에서는 **등급 수치 1곳**만 씁니다.

### 10.6 결과통보서 서식 토큰 (v4)

```css
--paper-bg:#E7EAF0;        /* 지면 바깥 캔버스 */
--form-line:#C7D0DE;       /* 표와 상자의 기본 괘선 */
--form-line-strong:#7F8FA9;/* 표 바깥선, 판정 상자 테두리 */
--form-head:#EEF1F6;       /* 표 머리, 스탯 타일 면 */
--form-zebra:#F8FAFC;      /* 20행 넘는 표의 짝수 행 */
--form-lead:#F6F8FB;       /* 안내문, 소견 블록 면 */
--zone-low:#F7DFDF;        /* 미달역 */
--zone-caution:#FAEEDA;    /* 주의역 (참고치 안쪽 아래 1/4) */
--zone-normal:#E6F0E9;     /* 정상역 */
--zone-high:#D2E6D9;       /* 우수역 (상한 초과) */
--zone-none:#EDEFF4;       /* 참고치 없음 */
```

v3의 `--app-bg` / `--card-line` / `--accent-blue` / `--accent-gold` / `--hero-*` / `--insight-*` 토큰은 폐기합니다.

**구역 경계 규칙.** 주의역은 참고구간 안쪽 아래 1/4 입니다. 중앙값을 경계로 쓰면 응시자 절반이 주의로 찍히고, 만점자가 많은 항목에서는 중앙값과 상한이 같아져 구역이 무너집니다.

### 10.7 인쇄 규칙 (@media print)

```css
@media print {
  @page { size: A4; margin: 0; }
  body { background: var(--surface); padding: 0; }
  .a4-page { width: 210mm; height: 297mm; margin: 0; border: 0; box-shadow: none; page-break-after: always; }
  .a4-page:last-child { page-break-after: auto; }
  .pdf-download-button, .pdf-loading-overlay { display: none !important; }
  table, .band, .rx-item, .wrong-item, .subject-item { break-inside: avoid; }
  .fsection-title, .page-head-title { break-after: avoid; }
}
```

- **판정과 구역 색은 정보이므로 인쇄에서도 유지합니다.** 히트맵 칸, 참고범위 구역, 플래그 뱃지, 네이비 문서 헤더에 `print-color-adjust: exact` 를 지정합니다.
- 흑백 프린터를 쓰는 독자를 위해 **색에만 의존하지 않습니다.** 정오는 O/X 글자를, 난이도는 상/중/하 글자를, 판정은 `▲ 우수` / `▼ 미달` 기호와 글자를 함께 표기합니다.
- PDF 내려받기는 `.a4-page` 를 한 장씩 캔버스로 떠서 A4에 얹습니다. 그래서 지면 높이 고정(10.2)이 기능 요구사항입니다.

### 10.8 보고서 지면 데이터 시각화 팔레트

10.7까지가 서식의 규칙이라면, 이 절은 **도판의 색 체계**입니다. v3에서 정한 팔레트를 그대로 유지합니다.

**이 팔레트는 보고서 지면 전용입니다.** 앱 UI(대시보드, 폼, 표)에 반입하지 않습니다. 앱은 2장 토큰만 씁니다.

#### 10.8.1 계층 분리

브라스 2곳 규칙(1.3)은 **성취 강조**에만 적용됩니다. 아래 시각화 색은 데이터를 구분하는 별도 계층이므로 브라스 예산에 포함되지 않습니다. 다만 시각화 색을 성취 강조로 쓰는 것은 금지합니다.

| 계층 | 역할 | 예산 |
|---|---|---|
| 네이비 (2장) | 구조와 본문 | 제한 없음 |
| 브라스 (1.3) | 성취 강조 | 지면당 2곳 |
| 판정 / 플래그 (10.3) | 참고치 대비 상태 | 규칙이 정한 곳에만 |
| 시각화 팔레트 (10.8) | 데이터 범주와 값의 구분 | 제한 없음, 단 데이터에만 |

#### 10.8.2 영역 시리즈 (범주 구분)

출제 영역마다 색을 고정 배정하고 **전 지면에서 같은 색을 씁니다.** 4쪽 도넛의 문학 색과 7쪽 표의 문학 칩 색이 달라지면 색 코딩이 무너집니다.

```css
--cat-1:#1B2942;  /* 딥네이비 */
--cat-2:#146B72;  /* 틸 */
--cat-3:#2C6B4F;  /* 포레스트그린 */
--cat-4:#B07A1E;  /* 앰버 */
--cat-5:#8C2F39;  /* 버건디 */
--cat-6:#5A6B87;  /* 슬레이트 */
```

배정은 데이터 순서가 아니라 **영역 이름 기준으로 고정**합니다. 이름이 팔레트에 없으면 순서대로 배정하고, 6개를 넘으면 슬레이트로 묶습니다. 서버 조판과 클라이언트 도판이 **같은 이름 -> 같은 토큰** 표를 각각 들고 있어야 합니다.

**계열 정렬은 이름으로 맞춥니다.** 레이더처럼 두 계열을 겹치는 도판에서 배열 순서로 맞추면, 정렬이 다른 두 배열이 만났을 때 다른 영역의 평균이 얹힙니다.

#### 10.8.3 난이도 색

기능색(fn-*)과 구분되는 지면 전용 3색입니다. 기능색은 시스템 상태용이고, 난이도는 시험지의 속성입니다.

| 난이도 | 토큰 | 뜻 |
|---|---|---|
| 상 | `--diff-hi` = `--cat-5` 버건디 | 어려움 |
| 중 | `--diff-mid` = `--cat-4` 앰버 | 보통 |
| 하 | `--diff-lo` = `--cat-3` 포레스트그린 | 쉬움 |

#### 10.8.4 정답률 히트 스케일

정답률을 면 색으로 읽히게 하는 5단계입니다. **옅은 면 + 진한 글자** 조합만 씁니다. 원색 대면적은 금지합니다.

| 구간 | 면 | 글자 |
|---|---|---|
| 0 ~ 39% | `--heat-1` #F6DCDE | `--heat-ink-1` #7A2731 |
| 40 ~ 59% | `--heat-2` #FAE7D2 | `--heat-ink-2` #8A5A12 |
| 60 ~ 74% | `--heat-3` #F3F0DF | `--heat-ink-3` #6B6535 |
| 75 ~ 89% | `--heat-4` #E4F0E2 | `--heat-ink-4` #2F5F41 |
| 90 ~ 100% | `--heat-5` #D3E7D8 | `--heat-ink-5` #1F5233 |

#### 10.8.5 도형 규칙

- **정오 히트맵**: 문항을 격자로 놓고 정답은 그린 계열 면, 오답은 버건디 계열 면, 난이도는 테두리 색으로 겹쳐 표시합니다. 한 도형에 세 정보(번호·정오·난이도)를 담되 네 번째는 넣지 않습니다.
- **참고범위 밴드**: 수치는 눈금 위에 띄우고, 눈금 안에는 구역 색과 마커만 둡니다.
- **성취 바**: 학생 값은 채운 바, 비교 대상(전체 평균)은 같은 축 위의 마커로 겹칩니다.
- **지브라 행**: 20행을 넘는 표에만 허용합니다. 5.4의 줄무늬 금지는 앱 UI 규칙이고, 45행짜리 분석표는 예외입니다.
- **캔버스 부모에 고정 높이**를 줍니다 (Chart.js ResizeObserver 폭주 방지).
---

## 11. 관리 화면(Operate) 문법

10장이 **학부모에게 건네는 문서**의 규칙이라면, 이 장은 **직원이 하루 종일 붙잡고 일하는 화면**의 규칙입니다. 학생 관리, 반 관리, 배포 관리처럼 목록을 훑고 걸러내고 처리하는 화면이 여기 해당합니다.

### 11.1 왜 따로 정하는가

사용자 피드백: **"디자인이 너무 AI 티가 난다."** 진단해 보니 원인은 색이나 활자가 아니라 **구조**였습니다. 목록 화면을 랜딩페이지처럼 만들고 있었습니다.

| 증상 | 실무 도구에서는 |
|---|---|
| 표를 카드로 감싸고 카드 헤더에 아이콘 + 제목 | 표가 본문 자체. 카드 없음 |
| 행 높이 57px, 여백 과다 → 한 화면에 11명 | 행 높이 44px 안팎 → 같은 높이에 툴바·페이저까지 |
| 필터·검색·정렬 없음 | 툴바가 상시 노출. 2,000명 규모의 전제 |
| 액션 버튼에 색 | 무채색 아웃라인. 색은 상태에만 |

**밀도는 미학이 아니라 기능입니다.** 한 화면에 몇 명이 보이는지가 그 화면의 실무 성능입니다.

### 11.2 규칙

- **관리 테이블은 카드로 감싸지 않습니다.** 표가 본문 폭 전체를 씁니다. 카드 헤더·아이콘·제목 장식을 붙이지 않습니다.
- **페이지 제목은 사이드바 활성 표시로 대체할 수 있습니다.** "학생 관리" 화면에 "학생 관리"라고 다시 쓰는 것은 지면 낭비입니다. 좌측 내비가 이미 어디인지 말하고 있습니다.
- **본문 최상단은 툴바입니다.** 가로 한 줄에 `[세그먼트 필터] [건수] ...여백... [검색] [주요 액션]` 순으로 놓습니다. 건수는 필터 결과를 반영하는 살아 있는 숫자여야 합니다.
- **행 높이는 데스크톱 44px를 목표로 합니다.** 셀 패딩은 `8px 12px` 안팎. 5.4의 `12px 16px`은 소량 표(10행 이하)의 규격이고, 관리 목록에는 이 장이 우선합니다.
- **행 액션은 무채색 아웃라인 소형 버튼입니다.** `--border` 테두리 + `--text-secondary` 글자. 파괴적 동작(삭제)만 예외로 `--fn-error` 글자를 허용하되 면은 칠하지 않습니다. 액션이 3개를 넘으면 버튼이 아니라 더보기 메뉴로 접습니다.
- **정렬 가능한 열은 머리를 버튼으로 만듭니다.** 정렬 중인 열만 방향 표시(▲/▼)를 답니다. 정렬 가능한 열은 1~2개로 제한합니다 - 전 열 정렬은 쓰이지 않으면서 머리만 시끄러워집니다.
- **50행을 넘길 수 있는 목록에는 페이지네이션을 답니다.** 클라이언트 페이지네이션 20행/쪽이 기본이고, 하단 중앙에 숫자 페이저를 둡니다. 필터가 바뀌면 1쪽으로 돌아갑니다.
- **정보 밀도를 위해 한 셀에 두 줄을 허용합니다.** 주 정보를 윗줄에, 부가 식별자를 아랫줄에 `caption` 크기로 둡니다(예: 연락처 + 아이디). 5.4의 한 줄 원칙은 이 장에서 완화됩니다.

### 11.3 모바일 (< 768px)

**관리 테이블은 모바일에서 가로 스크롤이 아니라 카드 리스트로 전환합니다.** 7열짜리 표를 375px에서 옆으로 밀게 하는 것은 읽을 수 있다는 뜻이지 쓸 수 있다는 뜻이 아닙니다.

- 카드 하나가 한 행입니다. 이름을 `body-lg` 600으로 세우고, 학년·학교를 `caption`으로 아래에 붙입니다.
- 액션은 카드 하단에 2개까지. 터치 타깃 44px를 지킵니다(11.2의 44px 행 높이는 데스크톱 규격이며, 모바일 터치 규정과 충돌하지 않습니다).
- 툴바는 세로로 접습니다. 검색이 가장 위, 세그먼트 필터는 가로 스크롤을 허용합니다.

### 11.4 폼 모달

필드가 6개를 넘는 모달은 **섹션 라벨 + 2열 그리드**로 밀도를 올립니다.

- 섹션 라벨은 `--surface-subtle` 면에 `caption` 700 `--text-secondary`로 얹은 얇은 띠입니다(예: "필수 입력 사항" / "선택 입력 사항"). 필수와 선택을 섞어 두면 사용자가 매번 별표를 훑어야 합니다.
- 2열 그리드가 기본이고, 긴 값(주소·메모)만 `span 2`로 폅니다.
- 하단 액션 줄은 **좌측에 위험도 낮은 보조 동작**(비밀번호 초기화 등), **우측에 주 동작**(저장)을 둡니다. 주 동작만 `--action` 면을 씁니다.

### 11.5 참조 범위 (중요)

실서비스 콘솔을 레퍼런스로 삼을 때 **가져오는 것은 레이아웃·밀도·정보구조·액션 문법뿐입니다.** 로고, 색, 문안, 아이콘, 브랜드 요소를 복제하지 않습니다. 사이드바는 우리 네이비를 유지합니다 - 레퍼런스가 흰 사이드바를 쓴다는 이유로 브랜드 정체성을 바꾸지 않습니다.
### 11.6 학생 중심 네비게이션 (지점 대시보드)

이 프로그램은 **학생 점수를 확인하는 도구**입니다. 그런데 초기 정보구조는 기능별 서랍(학생 관리 / 반 관리 / 배포 시험 / 보고서)이었고, "이 학생이 지금 몇 점인가"를 보려면 배포를 먼저 고르고 그 안에서 학생을 찾아야 했습니다. **주어와 목적어가 뒤바뀌어 있었습니다.**

**학생이 1급 객체입니다.** 학생을 먼저 고르고, 그 학생의 무엇을 볼지를 나중에 고릅니다.

#### 11.6.1 3층 구조

```
상단 GNB (네이비)   [성적 관리] [학생·반 관리] [시험 배포]     ← 업무 영역
좌측 학생 패널(흰색)  검색 / 학년 그룹 / 학생 목록              ← 대상 선택 (성적 관리에서만)
본문                학생 컨텍스트 탭 [응시 이력][성적 추이][보고서]  ← 무엇을 볼지
```

- **GNB는 네이비, 학생 패널은 흰색.** 브랜드 정체성(네이비)은 위쪽 띠가 지고, 오래 훑는 목록은 흰 바탕에서 읽습니다. 목록을 짙은 면에 올리면 스캔 비용이 올라갑니다.
- **주요 메뉴는 좌측이 아니라 상단입니다.** 좌측 폭은 학생 목록이 가져갑니다.
- **학생 미선택 상태의 본문은 지점 요약**입니다. 빈 화면을 두지 않습니다.

#### 11.6.2 학생 패널

- 검색은 이름 기준이면 충분합니다. 목록을 좁히는 도구지 조회 도구가 아닙니다.
- **접이식 그룹 + 우측 인원수.** 그룹 기준은 학년이 기본이고, 반 데이터를 학생 단위로 얻을 수 있을 때만 반 탭을 추가합니다. **데이터가 없으면 탭을 만들지 않습니다** - "준비 중" 탭은 만들지 않습니다.
- 학생 이름 우측에 **최근 등급 뱃지**(2.4 등급 색)를 답니다. 제출 기록이 없으면 `미응시`로 적습니다. 여기 들어가는 값은 전부 실제 응시 기록에서 나와야 합니다.
- 선택된 학생은 면(`--surface-subtle`) + 굵기로 표시합니다.

#### 11.6.3 본문 - 점수가 시선의 종착점

응시 이력 표에서 **점수는 가장 오른쪽, 가장 큰 활자**입니다. 왼쪽에서 시험명을 읽고 오른쪽 끝에서 점수를 만나는 흐름이 이 화면의 목적입니다. 등급 뱃지는 점수 바로 왼쪽에 둡니다.

- 상태는 글자로만 표시하고 면을 칠하지 않습니다. **`제출 완료`는 무채색**입니다(2.4). `작성 중`만 앰버, `미응시`는 `--text-tertiary`.
- 성적 추이는 **제출 완료된 응시만** 그립니다. 미응시를 0점으로 그리면 없는 하락을 만들어냅니다.

**학생 헤더에 요약 지표 한 줄을 답니다.** `응시 N회 · 평균 N점 · 최고 N점 · 최근 등급`. 학생을 고른 순간 상태가 한눈에 들어와야 합니다. **제출 완료 건만 집계**하고, 제출 기록이 없으면 수치를 만들지 말고 `응시 기록 없음`이라고 적습니다.

**행 액션은 그 행의 맥락에서 끝낼 수 있어야 합니다.** 응시 이력의 각 행 우측에 `[보고서 / AI 분석]`(보고서 유무에 따라 라벨이 바뀜)과 `[답안 입력]`을 무채색 아웃라인으로 둡니다. 이 동작을 하려고 다른 탭으로 옮겨 다니게 만들지 않습니다. 행 클릭(답안 패널 펼치기)과 충돌하지 않도록 액션 칸은 클릭 전파를 멈춥니다.

**배포됐지만 응시하지 않은 시험도 행으로 보여 줍니다.** 교사가 가장 알고 싶은 것은 친 시험이 아니라 **안 친 시험**입니다. 상태 `미응시`, 점수 `-`로 같은 표에 둡니다.

#### 11.6.4 답안 상세 패널

응시 이력의 행은 **끝이 아니라 입구**입니다. 점수를 본 다음 곧바로 따라오는 질문은 "그래서 뭘 틀렸나"이므로, 행을 눌러 그 자리에서 펼칩니다.

- **아코디언으로 펼칩니다.** 모달이나 우측 슬라이드가 아니라 행 아래 인라인 확장입니다. 목록의 위치를 잃지 않고, 모바일에서 별도 레이아웃이 필요 없습니다.
- **제출된 응시만 펼칩니다.** 미제출 행은 커서를 `default`로 두어 눌러도 열리지 않게 합니다. 열리지 않는 이유를 설명할 자리를 만드느니 애초에 눌리지 않게 합니다.
- 패널 머리: 시험명 · 제출일시 · 등급 뱃지 · **점수(큰 활자)** · 닫기. 그 아래 회색 띠에 `N문항 중 M문항 정답 · 취득 X점` 요약을 한 줄로 둡니다.
- **문항은 격자로 접습니다.** 45문항을 한 열로 세우면 지면이 끝없이 길어집니다. 폭에 따라 1 → 2 → 3열로 늘립니다.
- 문항 행: `문항번호 | 학생 답 | 정답 | 정오 | 배점`. **정오는 좌측 3px 컬러 바 + 우측 O/X 글자** 두 곳에서 읽히게 합니다. 정답 행만 옅은 성공색 면을 깔고 오답 행은 흰 면으로 둡니다 - 오답에 색을 칠하면 지면이 붉어집니다.
- 객관식 번호는 **원문자(①~⑩)**로 표기하고, 그 밖(주관식 등)은 값을 그대로 씁니다. 무응답은 `-`입니다.

**O/X 수동 채점 분기는 필수입니다.** `answers._gradingMode === 'ox'`인 기록은 값이 정답 번호가 아니라 O=1 / X=0입니다. 이때는 답 칸에 `O`/`X`를 적고 **정답 열을 숨깁니다**(지점이 종이 답안을 보고 채점한 기록이라 선택 답 자체가 없습니다). 이 분기를 빠뜨리면 O/X 기록이 "전원 1번 선택"으로 보입니다.

#### 11.6.5 데이터 규칙

**이 재편은 배치 변경이지 기능 추가가 아닙니다.** 학생 중심 화면은 이미 있던 쿼리 결과(배포별 학생 목록)를 **학생 기준으로 뒤집어** 만들고, 새 API를 부르지 않습니다. 서버 스키마가 주지 않는 것(반 소속, 재원 상태)은 화면에서 지어내지 않고 그 열을 만들지 않습니다.### 11.7 진행률과 자동 판정

관리 목록에는 **"얼마나 됐나"**를 묻는 열이 자주 생깁니다(응시 현황, 제출률, 채점 진행). 규칙을 하나로 둡니다.

- **숫자가 먼저, 막대는 보조입니다.** `12/30` 을 `tabular-nums`로 왼쪽에 두고, 그 오른쪽에 1.5px 높이의 막대를 붙입니다. 막대만 있으면 정확한 값을 읽을 수 없고, 막대가 두꺼우면 표의 행 높이를 밀어냅니다.
- 막대는 `--surface-subtle` 트랙 + `--action` 채움입니다. 진행률에 기능색(성공/경고)을 쓰지 않습니다 - 40%가 나쁜 값인지 좋은 값인지는 화면이 판단할 일이 아닙니다.
- 집계 데이터가 아직 없으면 `0/0`이나 `0%`를 그리지 말고 **`집계 중`**이라고 적습니다. 0은 "없음"이라는 사실 주장이 됩니다.

**상태는 서버 필드가 없으면 화면에서 판정하되, 판정 기준을 코드 주석에 남깁니다.** 배포 상태는 `현재 < 시작` → `예정`, `시작 ≤ 현재 ≤ 종료` → `진행 중`, `현재 > 종료` → `종료`입니다. 색은 2.4를 따라 **`진행 중`만 앰버**, 나머지는 무채색입니다 - `종료`는 실패가 아니라 정상 귀결입니다.

### 11.8 데이터가 없는 열은 만들지 않습니다

목록을 설계할 때 "있으면 좋을 열"을 먼저 그리고 나중에 데이터를 찾는 순서로 가면, 화면이 `-`로 가득 찬 표가 됩니다. **API가 주지 않는 값은 열 자체를 만들지 않고, 필요하면 서버 변경을 별도 과제로 올립니다.**

실제 사례: 반 관리 표에 `담당`과 `학생 수`를 넣으려 했으나 `classes` 스키마에 담당자 필드가 없고 목록 API가 학생 수를 주지 않았습니다(반마다 조회하면 N+1). 두 열을 빼고 `반 이름 / 학년 / 설명 / 상태 / 작업`으로 확정했습니다.
---

## 12. 검증 프로토콜

헌법을 지켰는지 **말로 주장하지 않고 명령으로 확인합니다.** NK 공통 시스템의 검증 절차를 이 프로젝트에 맞춰 옮겨 둡니다.

### 11.1 hex 0건 확인

색은 `client/src/index.css` 의 `:root` / `[data-theme="dark"]` 블록에만 존재합니다. 그 밖의 파일에 hex가 있으면 토큰 체계를 우회한 것입니다.

```bash
# 컴포넌트·페이지에 hex 리터럴이 없어야 한다 (index.css 는 토큰 정의처이므로 제외)
grep -rn "#[0-9a-fA-F]\{3,8\}\b" client/src --include=*.tsx --include=*.ts
```

출력이 없어야 통과입니다. 보고서 템플릿(`server/templates/`)과 정적 데모 산출물(`design-migration/*.html`)은 별도 지면이므로 이 검사 대상이 아닙니다.

### 11.2 상태는 색만으로 알리지 않습니다

등급·정오·성공/실패 등 모든 상태 표시는 **hue + 글자**를 함께 씁니다. 색각 이상 사용자와 흑백 인쇄에서도 읽혀야 합니다.

- 등급 뱃지: 기능색 면·테두리 + `N등급` 텍스트 (2.4)
- 토스트: 좌측 규칙선 색 + `성공`/`오류` 스크린리더 텍스트 (5.7)
- 참고치 이탈: 색 칩 + `미달`/`상회` 문구
- 금지: 빨간 점 하나로 오류를 표시하는 식의 색 단독 표기

### 11.3 FOUC 방지

테마는 **첫 페인트 전에** 확정합니다. `initTheme()` 을 `client/src/main.tsx` 최상단(React 렌더 이전)에서 1회 호출해 `data-theme` 을 붙입니다. 컴포넌트 `useEffect` 에서 테마를 적용하면 라이트로 한 번 그려진 뒤 다크로 바뀌는 깜빡임이 생깁니다.

이 프로젝트는 **SPA 단일 진입점**이므로 모듈 최상단 호출로 충분합니다. SSR·다중 진입점으로 바뀌면 인라인 `<script>` 로 옮겨야 합니다.

### 11.4 실행자 검증은 exit code 원문으로

"성공했다"는 서술은 근거가 아닙니다. 검증 보고에는 **명령과 종료 코드 원문**을 붙입니다.

```bash
npx tsc -p tsconfig.server.json --noEmit; echo EXIT=$?
npx tsc -p tsconfig.json --noEmit; echo EXIT=$?
npm run build; echo EXIT=$?
```

로그 마지막 줄이 성공처럼 보여도 종료 코드가 0이 아닐 수 있습니다(실제로 `vite build` 가 `✓ modules transformed` 를 출력하고도 `0xC0000409` 로 죽은 사례가 있습니다 — 분석서 §9.5). 화면 출력이 아니라 `EXIT=` 를 봅니다.

빌드는 **Node 22 LTS** 에서 수행합니다(`package.json` 의 `engines.node`).

---

## 부록. 화면 체크리스트

새 화면을 내보내기 전에 전부 확인합니다.

- [ ] 브라스가 정확히 2곳 이하인가
- [ ] 그라디언트가 0곳인가
- [ ] 색이 전부 토큰 참조인가 (hex는 `:root`와 `[data-theme="dark"]`에만)
- [ ] 등급 표시에 `--fn-error`를 쓰지 않았는가
- [ ] 숫자에 tabular-nums가 걸려 있는가
- [ ] 375px에서 가로 스크롤이 없는가
- [ ] 375px에서 사이드바가 오버레이 드로어인가
- [ ] 표에 가로 스크롤이 생긴다면 첫 열이 고정인가
- [ ] 라이트/다크 양쪽에서 열어 보았는가
- [ ] 자동 애니메이션이 0개인가
- [ ] 터치 타깃이 44px 이상인가
- [ ] 오류를 색이 아니라 문장으로도 알리는가
- [ ] em-dash와 이모지가 0개인가
